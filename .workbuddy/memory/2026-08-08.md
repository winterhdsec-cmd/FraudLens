# 2026-08-08 工作日志

## 16:30 电脑送修回来，继续修缮论文图
- **用户**：电脑送修回来了，继续修缮论文，先把图修好。
- **视觉自检发现**（上次交付的图 1/图 2，8 月 5 日版）：
  - panel 灰底太浅（gray!14）、阴影太淡（1.2pt），立体感不强；
  - 图 1 反思回环虚线箭头仍贴着输出栏边缘；
  - 图 1 analyze 容器内"analyze 分析"标题贴在外框上边；
  - 图 2 m4→d1 的箭头绕得别扭（要先右再上再左到顶部盒子）；
  - 两张图 panel 都没有深色标题条，缺少层次。
- **整改动作**：
  1. **panel 样式升级**：fill `gray!14` → `gray!22`，drop shadow `1.2pt/0.25` → `2.5pt/-2pt/0.4`，line width `0.6pt` → `0.8pt`，rounded corners `3pt` → `4pt`。
  2. **加 panel 标题条**（深灰底白字）：用 `fill=black!72, text=white` 的小条带放在每个 panel 顶部，类似刘莞玲图 2 风格。
  3. **图 1 反思回环优化**：向下偏移从 0.6cm 加大到 1.0cm，确保完全在引擎 panel 下方干净。
  4. **图 1 analyze 容器**：从白底升级为浅灰底（gray!6）+ 加粗边框；内嵌两个通道盒子也用浅灰底，更像子模块。
  5. **图 2 m4→d2**：把箭头终点从 d1（顶部盒子，需绕远路）改为 d2（中部盒子，与 m4 同高），绕路大幅减少。
  6. **标题字号**：\small\bfseries → \footnotesize\bfseries（更紧凑），text width 加大到 4.0cm 以避免 PDF 缩放后文字换行。
- **三轮迭代**：
  - v2：编译发现图 1"输出·服务·展示"和图 2 两个长标题在 PDF 中换行；
  - v3：标题字号改 \footnotesize，text width 加到 3.8cm，图 1"输出·服务·展示"修复，图 2"人机协同决策（学生介入）"仍换行；
  - v4：图 2 ptitle 继续加宽到 minimum width=4.2cm / text width=4.0cm，三个标题全部单行。
- **同步**：`fig_arch.tex` / `fig_workflow.tex` / `CPEC2026_draft.tex`（内联 TikZ）三处全部升级到新样式，`build_docx.py` 无需改（自动用新 PNG）。
- **编译验证**：
  - xelatex + bibtex + xelatex×2 全流程 **0 错误**；
  - PDF 14 页，`CPEC2026_draft.pdf` 成功写回（v4 → draft）；
  - PyMuPDF 渲染 PDF 第 5、6 页视觉自检：panel 立体感明显、深色标题条漂亮、三个长标题全部单行、箭头干净；
  - Word 版 `CPEC2026_draft.docx` 已重新生成。
- **文件变更**：`fig_arch.tex`（重写）、`fig_arch.pdf`、`fig_arch.png`、`fig_workflow.tex`（重写）、`fig_workflow.pdf`、`fig_workflow.png`、`CPEC2026_draft.tex`（两处内联 TikZ 升级）、`CPEC2026_draft.pdf`、`CPEC2026_draft.docx`。
- **清理**：删除 CPEC2026_v2/v3/v4/build/newbuild/final 共 32 个临时文件。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 16:45 用户给出三大评审意见，按意见彻底重构两张图
- **三条评审意见**：
  1. **布局缺层级与对齐**：模块摆放随意、间距不一、连线交叉多，应采用严格分层（左-中-右/上-下流式）。
  2. **视觉编码不统一**：颜色杂乱、字号粗细不一，应使用低饱和度色块区分层级、高亮色仅用于关键路径、字号字体统一、留白充足。
  3. **信息密度不均**：单张图塞太多细节，应做减法、用标准化组件、子图折叠次要信息。
- **图 1 整改为"严格分层架构"**：
  - 重写为上下分层：数据层 → 预处理层 → 研判引擎 → 应用/服务层 → 支撑底座
  - 每层一个等宽 panel，标题条统一深灰白字
  - 层间箭头走左列 x=-4.2 垂直线，零交叉
  - 引擎五节点横排 + analyze 容器（含 HAN 双通道"结构通道/金额加权图"+"文本通道/BGE 嵌入"）
  - 反思回环放引擎行下方 U 型，标签"条件边回连"不压 ptitle
  - 标题绝对坐标 y=5.85 放最顶部
- **图 2 整改为"严格三栏网格对齐"**：
  - 三栏顶部标题条 y=2.4 严格对齐
  - 盒子按网格线 y=1.5/0.3/-1.0 严格水平对齐
  - 跨栏箭头全纯水平（s1→m1、s2→m2、m3→d3），零绕路
  - 反馈条 fb 居中底部，反馈箭头正交 `|-` `-|` 走
  - m3 内容精简为 4 行（标题 + 流程 + 输出候选），mbox 高 1.55cm
- **迭代细节**：
  - v2：标题 path 中点压预处理层 → 改绝对坐标
  - v3：反思回环上方版压 ptitle → 改为下方 U 型
  - v4：通道盒子窄导致"金额加权图"换行 → chbox 加宽 1.6cm、awrap 3.8cm
  - v5："研判引擎" ptitle 与 awrap 重叠 → ptitle y 上移到 -0.4
  - v6：PDF 缩放后图 2 三个长标题换行 → ptitle 加宽到 4.0cm
- **统一视觉编码**：
  - 全灰阶：深灰标题条 (black!70) / 浅灰 panel 底 (gray!22) / 白盒模块
  - 字号统一 \footnotesize（盒内副标题用 \scriptsize）
  - 留白加大：panel inner sep 12pt、盒子 min height 0.62cm、层间距 2.6cm
- **同步**：fig_arch.tex / fig_workflow.tex 重写，CPEC2026_draft.tex 内联 TikZ 同步，build_docx.py 无需改（自动用新 PNG）。
- **编译验证**：xelatex+bibtex 全流程 **0 错误**，PDF 14 页，CPEC2026_draft.pdf 成功写回（v6→draft），Word 版重生。
- **文件变更**：fig_arch.tex、fig_workflow.tex、CPEC2026_draft.tex、CPEC2026_draft.pdf、CPEC2026_draft.docx。
- **清理**：删除 v2-v6 + build/newbuild/final 共 23 个临时文件。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 19:42 按用户详细评审意见彻底重画图 1（系统架构）
- **用户给了刘莞玲图 2 作参照 + 9 条文字问题清单**，逐条整改 `fig_arch.tex`：
  1. **逻辑数据流**：
     - 反思条件回环改为引擎层内部 U 型闭环（y=-4.15），标签移在线段**上方**"置信度低 / 规则触发 → 返回规划"，不再压边界、明确触发条件与回连到"规划"；
     - 新增**自下而上 Human-in-the-loop 反馈闭环**：图左侧外部绕行（a6 人工审核 → d1 案情库），虚线，标签"人工审核反馈（Human-in-the-loop）"；修掉了上一版 `-|`/`|-` 方向 bug（原会把箭头画到图中央穿盒）；
     - "合成案情"不再并列为最底层源，改名"AMLSim 合成数据（Synthetic）"归入数据层三源之一。
  2. **视觉排版**：
     - 全图统一"中文（English）"格式，消除 plan/预处理 等硬拼写法；
     - 核心"智能研判引擎"panel 用 **blue!9 淡蓝高亮 + 加粗边框**，其余灰底，重点突出；
     - 应用层 2×3 等宽等高盒（2.8cm）；引擎控制节点 e1/e2/e4/e5 与 analyze 容器间距修正（gap 5.6pt，不再重叠）；
     - 主数据流改走左侧脊柱 x=-3.0，避免压盖居中标题条；标题条最后绘制以覆盖被穿线段；
     - 底部支撑改为**有边框独立条带**（数据与模型支撑 / 工程可信保障）。
  3. **内容技术**：
     - 术语颗粒度分层：控制流（规划/预处理/聚类/反思）横排，分析（Analyze）容器内含"结构通道（金额加权图）/文本通道（BGE 嵌入）"算子，不再同级并列；
     - **删除重复的"多级降级链"**（原应用层+底部各一处，仅留应用层）；
     - 底部技术栈改**通用术语**（关系型数据库/缓存中间件/本地嵌入模型/云端大模型 + RBAC鉴权/审计溯源/可解释性），不再写死 MySQL/Redis/Vue3。
- **同步**：`_sync_figs.py` 提取入 `CPEC2026_draft.tex`（fig:arch）；xelatex+bibtex 全流程 0 错误（临时 jobname 编译）。
- **坐标级验证**（PyMuPDF）：四 panel 宽均 385.5pt、x0=22.9/x1=408.4/cx=215.7 完全一致；各层盒子以 213.2 中心对称；引擎盒对称（74.3/142.3/284.0/352.1）；analyze 容器居中 cx=213.2；标题全部单行不换行；反馈闭环在 panel 左缘内侧 26.6pt 运行、不穿盒。
- **交付**：因 `CPEC2026_draft.pdf` 被阅读器占用无法覆盖，另存 `CPEC2026_draft_图1修订.pdf`（15 页）并重建 `CPEC2026_draft.docx`、`fig_arch.pdf`、`fig_arch.png`；临时 aux 已清理。
- **回归风险**：仅改图 1 内联 TikZ，正文未动；§3 文字已与图一致（合成≠真实、反思闭环、两级闭环、工程可信四条）。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 17:30 用户反馈"图一直歪"，定量坐标分析后彻底修复
- **用户**："无论你怎么改，为什么这个图一直都是歪的？反思一下，并且马上改好。"
- **反思**：之前一直靠"肉眼 patch"，没有定量验证对齐。用 PyMuPDF 提取 PDF 中所有矩形坐标，找到真正歪的根源：
  - **图 1**：应用层 panel 中心偏移到 x=171.8（其它层 211.5，偏左 40pt）——应用层只有 3 列盒子（到 x=1.4），fit 后中心偏左；引擎层 panel 宽 376 vs 其它 319——引擎 5 节点横排更宽。
  - **图 2**：三栏 panel 底部不齐（学生栏底 149 / 系统栏 180 / 决策栏 170）；且 `fit + minimum height` 会让内容在 panel 内**垂直居中**，矮栏顶部也偏高。
- **根本修复**：
  - **图 1**：所有 panel 用 `fit + minimum width=13.6cm` 统一宽度 → 四层 panel 全部 cx=208.3、宽 385.5pt 完全一致；引擎层节点严格对称（x=-5.2,-2.6,0,2.6,5.2，awrap 中心归 0）；应用层改 4 盒对称行 + 2 盒居中行；层间箭头改 x=±4.2 **双侧对称**。
  - **图 2**：三栏 panel **弃用 fit，改显式矩形**（minimum width=4.6cm, minimum height=5.4cm, 中心 y=0.4）→ 三栏顶/底/高完全一致（顶 33.3 / 底 186.3 / 高 153.0pt）；盒子按网格线严格对齐。
- **验证**：PyMuPDF 定量确认——图 1 四 panel 中心/宽度完全一致；图 2 三 panel 顶/底/高完全一致，三行盒子水平对齐（±0.2pt）。
- **同步**：新增 `_sync_figs.py` 脚本自动把 standalone 的 tikzpicture 提取替换进主稿（避免手抄出错）；xelatex+bibtex 0 错误，PDF 14 页；Word 重生。
- **经验沉淀**：TikZ 对齐不要用 `fit` 自适应宽度/高度（fit+min height 会居中内容导致顶部偏移）；用 `fit + minimum width`（图 1）或**完全显式 panel 矩形**（图 2）；对齐验证用 PyMuPDF 提取矩形坐标定量检查，不要靠肉眼。
- **文件变更**：fig_arch.tex、fig_workflow.tex、CPEC2026_draft.tex、CPEC2026_draft.pdf、CPEC2026_draft.docx；新增 `_sync_figs.py`（保留）。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。
## 20:30 用户要求“自己看图一有哪些问题，自己重新改”
- **自审发现的问题**（对照上一轮交付版）：
  1. 核心引擎 panel 用了 `blue!9` 淡蓝，违反用户此前“黑白画”要求，且黑白打印对比度差；
  2. 图内顶部有 `FraudLens 反诈团伙研判系统总体架构` 大标题，与 LaTeX caption 重复，增加信息密度；
  3. 层间主数据流（原始数据/清洗数据/研判结果）的左侧脊柱没有明确从具体模块发出、进入具体模块，存在“箭头指不到方框”问题；
  4. 人工反馈闭环从 `a6.south` 出发且标签压到引擎层左侧，可读性差；
  5. “案情结构化（Case Struc-turing）”英文换行断裂；
  6. 引擎层内部预处理→分析、分析→聚类是斜向箭头，不如正交清晰；
  7. 反思回环 U 型底边偏低，贴近应用层标题条。
- **整改动作**（`fig_arch.tex` 重写 + 多轮自审迭代）：
  - 移除图内大标题，改纯灰阶：`panelcore` 用 `gray!35` 深灰强调核心引擎，其余 `gray!22` 浅灰，标题条 `black!70` 白字；
  - 层间数据流改为明确连接代表节点：`案情库 → 字段对齐`（原始数据）、`数据脱敏 → 预处理`（清洗数据）、`反思 → 人工审核`（研判结果）；
  - 引擎内部控制流改为正交：`e2.east -- ++(0.20,0) |- (ana.north)`、`ana.east -- ++(0.20,0) |- (e4.west)`；
  - 反思回环 U 型整体上移（rbot/rright y=-3.45），标签分置“置信度低 / 规则触发”与“返回规划”；
  - 人工反馈闭环从 `a6.north west` 出发，先上到应用层标题条上方（y=-4.10）再左行，标签置于左上角空白处（`text width=3.0cm`），不压任何 box；
  - 预处理英文改用缩写（Schema / Desensitize / Format Conv / Case Struct）避免换行。
- **自审中间发现的回归问题并修复**：
  - “研判结果”实线从 e5 中心垂直下来会穿过 a3“审计日志”box → 改从 `e5.south east` 右侧外部下行，指向 `a6.north`；
  - 反馈标签 `text width=2.4cm` 导致 `Human-in-the-loop` 被截断 → 加宽到 `3.0cm`；
  - 反馈线横段 y=-4.4 擦到应用层 ptitle → 上移到 y=-4.10；
  - 引擎层 panel 底部贴近应用层 ptitle → rbot 上移后层级间隙清晰。
- **坐标级验证（PyMuPDF）**：
  - 四 panel 全部 `x0=20.5`、`x1=406.0`、`cx=213.2`、`w=385.5pt`，完全一致；
  - 数据层盒子中心 `[114.0, 213.2, 312.4]`、预处理层 `[88.5, 170.7, 255.7, 338.0]`、引擎层 `[74.3, 142.4, 284.1, 352.1]` 均以 213.2 对称；
  - 应用层上下排中心 `[99.8, 213.2, 326.6]` 对称。
- **同步与编译**：`_sync_figs.py` 提取新 TikZ 入主稿；临时 jobname `build_tmp` 编译 xelatex+bibtex 全流程 0 错误，PDF 14 页；`fig_arch.png` 与 `CPEC2026_draft.docx` 已重生。
- **交付**：`CPEC2026_draft_图1重绘.pdf`（因 `CPEC2026_draft.pdf` 仍被阅读器占用）、`fig_arch.pdf`、`fig_arch.png`、`CPEC2026_draft.docx`。
- **文件变更**：`fig_arch.tex`、`fig_arch.pdf`、`fig_arch.png`、`CPEC2026_draft.tex`（内联 TikZ 同步）、`CPEC2026_draft_图1重绘.pdf`、`CPEC2026_draft.docx`。
- **清理**：删除 `fig_arch_*_selfcheck.png` v1-v7 共 7 个自审临时图；删除 `build_tmp.*` 编译残留。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 21:20 用户再次指出“图很乱、内容一般”，并提供两张参考图要求模仿
- **用户反馈**：当前图 1 / 图 2 “格式很乱并且内容很一般”，截来 LLM+NeuralCDM 图 3 与 LLMDS 图 2 作参照，要求模仿别人的画法重画。
- **参考图风格提炼**：浅灰模块底（gray!18~25）、细黑边框（0.5pt）、小圆角、标题内置加粗、模块按输入→处理→输出横向展开、箭头正交简洁、少标签少嵌套。
- **图 1 重构为“七阶段横向流水线”**：
  - 数据源 → 预处理 → 规划 → 分析 → 聚类 → 反思 → 应用服务；
  - 核心阶段（规划/分析/聚类/反思）用 `gray!25` 深灰强调，其余 `gray!18`；
  - 分析阶段内置“结构通道 / 文本通道”双通道；
  - 反思回环在引擎下方局部 U 型返回规划；
  - 人工审核反馈从应用服务顶部横跨回数据源；
  - 底部保留两条支撑条（数据与模型支撑 / 工程可信保障）。
- **图 2 重构为“三栏横向流程”**：
  - 学生·实验操作 / FraudLens 系统内核 / 人机协同决策；
  - 系统内核用 `gray!25` 深灰强调；
  - 三栏内盒子严格水平对齐（y=1.50/0.30/-1.05）；
  - 跨栏箭头纯水平，仅保留“候选结果”等必要标签；
  - 底部评价反思回流条 + 决策记录 / 评价回流箭头。
- **迭代修复的关键问题**：
  - 图 1 早期版本 panel 拥挤、标题压盒、箭头标签压文字 → 多次加宽模块间距、精简标签、移除图内大标题；
  - 图 2 早期版本中间栏拥挤、箭头标签压盒、`·` 导致文字断行 → 加宽中间 box、`·` 改 `/`、删除冗余标签。
- **同步与编译**：`_sync_figs.py` 同步两图内联 TikZ；临时 jobname `build_tmp` 编译 xelatex+bibtex 全流程 0 错误，PDF 14 页；`fig_arch.png`、`fig_workflow.png` 与 `CPEC2026_draft.docx` 已重生。
- **交付**：`CPEC2026_draft_图1图2重绘.pdf`（因 `CPEC2026_draft.pdf` 仍被阅读器占用）、`fig_arch.pdf/png`、`fig_workflow.pdf/png`、`CPEC2026_draft.docx`。
- **文件变更**：`fig_arch.tex`（彻底重写为横向流水线）、`fig_workflow.tex`（改为参考图风格三栏）、`CPEC2026_draft.tex`（两处内联 TikZ 同步）、`fig_arch.pdf/png`、`fig_workflow.pdf/png`、`CPEC2026_draft_图1图2重绘.pdf`、`CPEC2026_draft.docx`。
- **清理**：删除 `fig_arch_refstyle_*` v1-v12 与 `fig_workflow_refstyle_*` v1-v6 共 18 个自审临时 PNG。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 22:20 用户指出“图一有文字重叠、图二问题比较大”，定位修复
- **用户截图反馈**：
  - 图 1 有文字重叠（在嵌入 PDF 中表现为“研判结果”标签压到“反思/应用服务”盒顶、“置信度低 / 规则触发”贴近盒底）；
  - 图 2 问题较大：三栏 panel 标题完全缺失（被裁切），底部“决策记录”标签压在反馈条文字上。
- **图 1 修复（`fig_arch.tex`）**：
  - 把核心阶段盒（规划/聚类/反思）适当加宽，聚类盒加宽到 1.8cm 以容纳 `Cluster`；
  - 统一各盒间水平间距为 0.5cm，整体宽度控制在 13.8cm 左右，避免嵌入后过度缩放；
  - **删除引发重叠的“研判结果”箭头标签**，主数据流不再堆砌标签；
  - 反思回环标签改为：水平段下方写“置信度低 / 规则触发”，垂直上升段左侧写“返回规划”，均远离右侧应用服务盒；
  - 人工反馈闭环标签上移并加 yshift，避免压 dashed line。
- **图 2 修复（`fig_workflow.tex`）**：
  - 原因：`fit` panel 配合 `label={[anchor=north]above:...}` 会把标题画进 panel 内部并被背景层覆盖。改为 panel 节点不配 label，随后用三个显式 `
ode[title, anchor=south]` 放在各 panel 正上方；
  - 底部“决策记录”标签从 `|- (fb.east)` 改为先垂直到 `fb.north` 再水平到 `fb.north east`，标签写在垂直段右侧、bar 上方，不再压条内文字；
  - bar 宽度从 13.4cm 微调，与图 1 底部支撑条风格一致。
- **验证**：
  - 用 PyMuPDF 分别检测 `fig_arch.pdf` 和 `fig_workflow.pdf`，文本线框交集面积为 0；
  - 嵌入主稿后重新渲染第 4/5 页目视检查：图 1 无叠字、箭头间隙均匀；图 2 三栏标题清晰、底部标签独立。
- **同步与编译**：`_sync_figs.py` 同步；`build_tmp` 临时 jobname 编译 xelatex+bibtex×3 全流程 0 错误，PDF 14 页；PNG/Word 已重生；`CPEC2026_draft.pdf` 成功写回。
- **交付**：`CPEC2026_draft_图1图2修复.pdf`、`fig_arch.pdf/png`、`fig_workflow.pdf/png`、`CPEC2026_draft.docx`（主 PDF 已同步为同一内容）。
- **文件变更**：`fig_arch.tex`、`fig_workflow.tex`、`CPEC2026_draft.tex`（内联 TikZ 同步）、`fig_arch.pdf/png`、`fig_workflow.pdf/png`、`CPEC2026_draft.pdf`、`CPEC2026_draft_图1图2修复.pdf`、`CPEC2026_draft.docx`。
- **清理**：删除 `fig_arch_fix_*`、`fig_workflow_fix_*`、`verify_page_*` 等临时 PNG。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 22:55 用户指出“方框重叠、文字重叠、文字出框”，量化修复
- **用户反馈**：两张图都有共性问题——方框重叠、文字重叠、文字到方框之外。
- **量化定位（PyMuPDF）**：
  - 图 1：`预处理` 与 `规划` 方框实际重叠约 0.08 cm（中心距 = 半宽之和，触边）。
  - 图 2：`LangGraph 反思闭环` 盒内 `plan→preprocess→analyze→cluster→reflect` 单行溢出盒外。
- **图 1 修复**：
  - 全部 stage 统一 `minimum width=2.0cm`、`text width=1.7cm`、`minimum height=2.5cm`；
  - 中心坐标按 -7.2 / -4.8 / -2.4 / 0.0 / 2.4 / 4.8 / 7.2 排布，相邻盒中心距 2.4 cm，保证 0.4 cm 清晰间隙；
  - 反思回环下移到 y=-2.1，标签全部放在留白区；人工反馈闭环在 y=1.9；底部支撑条下移到 y=-3.0 / -3.65。
- **图 2 修复**：
  - `LangGraph` 盒加宽到 5.0 cm，流程文字拆为两行（`plan→preprocess→analyze` / `cluster→reflect`），严格在盒内；
  - 三栏 panel 与内部盒子重新定宽，跨栏箭头完全走 panel 间隙；
  - “候选结果”“决策记录”“评价 / 反思回流”三个标签均置于留白或 bar 上方，不压 panel / bar。
- **验证**：
  - PyMuPDF 检测：内容方框之间无重叠，内容文字全部在各自方框内，文字-文字无重叠；
  - 主稿第 4/5 页目视检查无叠字、无压线、无溢出。
- **同步与编译**：`_sync_figs.py` 同步；xelatex+bibtex×3 全流程 0 错误，PDF 14 页；主稿 `CPEC2026_draft.pdf` 成功写回；Word 已重生。
- **交付**：`CPEC2026_draft_图1图2去重叠.pdf`、`fig_arch_去重叠.pdf`、`fig_workflow_去重叠.pdf`、`CPEC2026_draft.docx`。
- **文件变更**：`fig_arch.tex`（重写）、`fig_workflow.tex`（重写）、`CPEC2026_draft.tex`（内联 TikZ 同步）、`fig_arch.pdf/png`、`fig_workflow.pdf/png`、`CPEC2026_draft.pdf`、`CPEC2026_draft.docx`。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 23:20 用户要求“图二还是有问题，你用视觉继续解决”
- **用户反馈**：图二仍有问题，未指具体位置，要求 AI 用视觉继续解决。
- **视觉自检发现的主要问题**：
  - 左栏只有两个 box，底部大片留白，三栏视觉不平衡；
  - box 几乎贴满 panel，跨栏箭头看起来像指向 panel 边框而非具体 box；
  - 底部“决策记录”箭头从 d3 下来后向右拉到 feedback bar 最右端，逻辑别扭；
  - `LangGraph 反思闭环` 盒（m3）比右栏底盒低，三栏底部不齐；
  - m2 副标题“语义 / 结构双通道”与 m3 标题“LangGraph”存在文字重叠（PyMuPDF 确认 ov=91）。
- **图 2 重构动作（`fig_workflow.tex` 重写）**：
  - 三栏改为**每栏三个 box**，左栏新增“提交案情 / 启动”，视觉平衡；
  - 统一 panel 宽 3.8cm、高 4.2cm；普通 box 宽 3.2cm；m3 宽 3.5cm 以容纳两行英文流程；
  - box 与 panel 边框保留约 0.3cm 清晰边距，跨栏箭头明确从 box 指向 box；
  - 垂直坐标调整为 y=1.5/0.2/-1.3，保证 m2-m3 间距 0.5cm 以上，消除文字重叠；
  - “决策记录”箭头改为从 d3 垂直进入 feedback bar 正上方（`d3.south |- fb.north`），不再右拉；
  - “案情输入”标签放在 s3→m3 跨栏箭头上，“候选结果”放在 m3→d3 箭头上，均悬空不压盒。
- **验证**：
  - PyMuPDF 精确检测：9 个内容 box 之间无重叠；内容文字全部在各自 box 内；文字-文字无重叠；
  - m3 内“plan→preprocess→analyze”右边距 6.4pt（0.23mm），已脱离贴边风险；
  - 主稿第 5 页目视检查：三栏对齐、无叠字、无压线、反馈箭头逻辑顺畅。
- **同步与编译**：`_sync_figs.py` 同步；xelatex+bibtex×3 全流程 0 错误，PDF 14 页；主稿 `CPEC2026_draft.pdf` 成功写回；Word 已重生。
- **交付**：`CPEC2026_draft_图2视觉重排.pdf`、`fig_workflow_视觉重排.pdf`、`CPEC2026_draft.pdf`、`fig_workflow.pdf/png`、`CPEC2026_draft.docx`。
- **文件变更**：`fig_workflow.tex`（重写）、`CPEC2026_draft.tex`（内联 TikZ 同步）、`fig_workflow.pdf/png`、`CPEC2026_draft.pdf`、`CPEC2026_draft.docx`。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 23:41 用户指出图 2 系统内核盒子“交界”、候选结果箭头歪、标签贴框边
- **用户反馈**（语音）：
  - 图 2 第二个大方框（FraudLens 系统内核）内部，框与框之间有“交界”/贴边；
  - “候选结果”箭头是歪的；
  - 不希望“候选结果”“案情输入”文字触碰到方框边缘；
  - “决策记录”也不要触碰方框边缘；
  - 整体已差不多，要求微调：间距拉开、字不贴边、系统内核内三框不要太大。
- **量化定位与根因**：
  - 上一版系统内核 box 宽 3.2/3.5 cm，panel 仅 3.8 cm，左右边距只剩 ~4 pt，视觉上像贴满 panel；
  - m3 与 d3 y 坐标差 0.1 cm，导致跨栏箭头轻微倾斜；
  - “案情输入/候选结果”标签放在 arrow `midway`，正好压在 m3/s3 的横向范围内，距 box 边仅数 pt。
- **图 2 最终调整（`fig_workflow.tex`）**：
  - 三栏 panel 统一加宽到 4.0 cm、加高到 5.5 cm，内部留白显著增加，盒子不再“撑满”；
  - 系统内核三盒统一使用 `mbox` 样式，宽 3.6 cm、高 0.78 cm，彼此间垂直间隙 ≥ 14 pt（≥0.5 cm），彻底消除“交界”感；
  - m3 与 d3 统一放在 y=-1.6，跨栏箭头 `m3.east → d3.west` 与 `s3.east → m3.west` 完全水平；
  - 三个跨栏/反馈标签全部带 `fill=white` 白底，并悬空放置：
    - “案情输入”“候选结果”提升到箭头**上方 0.70 cm** 的干净空白处；
    - “决策记录”放在 d3 到 feedback bar 的竖箭头右侧、bar 上方；
  - 普通 box 加宽到 3.2 cm / text width 2.7 cm，内容文字到框边最小边距 ≥ 1.6 pt；
  - 底部反馈条与三个 panel 保持清晰间距。
- **验证**：
  - PyMuPDF 精确检测：9 个内容 box 之间无重叠；标签到最近 box 距离 ≥ 9.9 pt；
  - 内容文字全部在各自 box 内，文字-文字无重叠；
  - 主稿第 5 页嵌入后目视检查：三栏对齐、内核三盒间距均匀、箭头水平、标签不压框/线。
- **同步与编译**：`_sync_figs.py` 同步；xelatex+bibtex×3 全流程 0 错误，PDF 14 页；主稿 `CPEC2026_draft.pdf` 成功写回；Word 已重生。
- **交付**：`CPEC2026_draft_图2微调.pdf`、`fig_workflow_微调.pdf`、`CPEC2026_draft.pdf`、`fig_workflow.pdf/png`、`CPEC2026_draft.docx`。
- **文件变更**：`fig_workflow.tex`（重写）、`CPEC2026_draft.tex`（内联 TikZ 同步）、`fig_workflow.pdf/png`、`CPEC2026_draft.pdf`、`CPEC2026_draft.docx`。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。

## 图 2 美观精修收尾（盒高统一 + 标签不压框）
- **残留问题**：m3 第三行 `analyze→cluster→reflect` 因 `text height/depth` 强制单行基线，三行内容溢出盒底 ~3.1 pt；"评价/反思回流"标签压到底部 bar（重叠 ~3.3 pt）；"案情输入/候选结果"标签放在跨栏箭头 `above`，实际压到底排 box（s3/m3）边缘。
- **根因**：`box`/`mbox` 的 `text height=0.75em, text depth=0.45em` 把多行文字按单行基线居中，导致 3 行 m3 溢出；跨栏箭头沿 box 边缘走，标签 `above` 后被推入 box。
- **修复（`fig_workflow.tex`）**：
  - 删除 `box`/`mbox` 的 `text height/depth`，统一 `minimum height=1.25cm`，靠 `minimum height` 驱动 9 盒等高；m3 行距微调（`\[-1pt]`/`\[-3pt]`）。
  - 底部 feedback bar 从 `y=-3.25` 下移到 `y=-3.45`，在 panel 底与 bar 之间留出标签空间；"评价/反思回流"标签改放到 `(-6.3,-2.86)` 锚 `west`，落在 bar 与 panel 顶之间的空白带，距 bar 4.7 pt、距 panel 顶 3.7 pt（均不重叠）。
  - "案情输入/候选结果"标签由 `above=14pt` 改为 `below=24pt` 且 `pos=0.5`（居中于栏间空白带下方），彻底脱离底排 box（垂直间距 ≥3.5 pt），不再压框。
- **量化验证（PyMuPDF）**：9 盒等高 35.4 pt（1.25 cm）、3 panel 等高 153.1×107.7 pt；内容文字全部在盒内（溢出 0）；4 个浮动标签（案情输入/候选结果/决策记录/评价·反思回流）与所有 box/bar **零重叠**；图宽 13.47 cm 适配 A4 正文。
- **构建**：`_sync_figs.py` 同步 → xelatex+bibtex×3 0 错误，主稿 14 页；Word 重生；`fig_workflow_美观精修.png` 重渲；`CPEC2026_draft_图2美观精修.pdf` 覆盖。
- **交付文件**：`CPEC2026_draft.pdf`、`CPEC2026_draft.docx`、`fig_workflow.pdf`、`fig_workflow_美观精修.png`、`CPEC2026_draft_图2美观精修.pdf`。
- **仍待补**：吴燕波老师出生年月日、胡永清院级科研基金编号。
