# 上台前速查卡（1 分钟扫完）

## 一、服务状态（刚实测）

| 服务 | 端口 | 状态 |
|---|---|---|
| 后端 API | **5003** | ✅ **正常**（/docs 返回 200，有活动连接） |
| 前端 dev | **5173** | ❌ **没有在跑** —— 必须先启动！ |

**启动前端（在项目根双击或命令行都行）：**
```
cd E:\FraudLens\frontend
npm run dev
```
启动后浏览器开 **http://localhost:5173**，登录 `admin / admin123`。

> ⚠️ 别用根目录的 `run-local.bat`：它会**再起一个后端**，和已在跑的 5003 冲突。只起前端即可。

---

## 二、演示时请绕开这些模块（对应的表是空的）

实测：库里这 9 张表**一条数据都没有** —— 进去就是"暂无数据"：

- **accounts**（涉案账户）、**persons**（人员）、**phones**（号码）
- **evidence_items**（证据链）
- **review_opinions**（复核意见）
- **freeze_approvals / freeze_receipts**（冻卡审批 / 回执）
- **merge_suggestions**（并案建议）
- **imported_fund_flows**（导入流水）

**建议演示动线（都有数据的）：**
团伙列表（49 个）→ 团伙详情 → 案件列表（230 起）→ 告警中心（87 条）→ 研判任务（3 项）→ 审计日志（1370 条）

> 其中「案件-团伙关联证据」刚刚已修好，每起案件都会显示具体归团伙理由。

---

## 三、刚修好的一个东西

「案件-团伙关联证据」原先 230 条记录的**理由字段全是空的**，所以卡片看着像空白。
已按真实字段回填 —— 现在会显示：
`话术特征命中：冒充公检法；归入团伙「冒充公检法犯罪团伙4号」；语义聚类相似度 0.85`

**看之前请刷新页面**（前后端都有 30–60 秒缓存）。

---

## 四、如果被追问

| 问 | 答 |
|---|---|
| 数据都是模拟的吗？ | 主体是六种自建模拟场景；外部验证含**真实 Elliptic 链上交易图（20 万笔）**。真实图上所有方法都失效，我们据此改用"扩线"设定。 |
| 企业里到底做了什么？ | 提供**多源数据关联分析**能力，集成进他们的信息化/安防项目。**不要说"在企业做了反诈研判"**。 |
| 3 家企业都落地了吗？ | **2 家已投用，1 家（山东世纪恒安）完成技术调研与方案研讨**。 |

**通用兜底**：答不上就说"这个点我们还没做到，是下一步要补的，我现在能确定的是……"
**两条禁忌**：不现场编数字；不说"大概/应该"。
