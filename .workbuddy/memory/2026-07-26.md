# 2026-07-26 开发日志

## Stage 2 收尾（#12 资金回流闭环检测 + #9 可冻卡清单/置信度门控）
- 按《FraudLens_需求与开发流程总览.md》验证驱动顺序继续：#6(已完) → #12 → #9（纯代码，沙箱可验证，不依赖 Docker 验收门）。
- 改动点：
  - graph_builder：新增 `get_fund_flow_digraph()`（抽取 fund_flow 有向子图，方向由边属性 from/to 决定）；`build_graph` 入口重置 `node_to_idx/idx_to_node` 修复重复构建 KeyError（R10 防御性）。
  - synthetic_data：新增 `reflux_cycle_prob` 参数，UP→收款账户回流边制造已知闭环（acc→hub→up→acc），供 #12 单测。
  - gang_detector：新增 `detect_reflux_cycles`（沿 fund_flow 正/反向可达闭包过滤团伙闭环，修种子账户漏 HUB/UP 的过滤 bug）；`_generate_gang_info` 增 reflux_cycles/is_reflux/freeze_candidates/confidence(0.3规模+0.2资金+0.2账户+0.3回流)/gate_decision(≥0.5→建议冻结否则待人工复核，契合 B5 绝不模型猜测定冻卡)。
- 冒烟 `smoke_stage2.py` 全通过：①有向子图检 5 闭环，示例 HUB→UP→ACC→...；②5/5 团伙标 is_reflux；③#9 字段齐全，高置信示例 conf=0.928 gate=建议冻结；④无回流数据 0 误报；⑤弱团伙 conf=0.143 gate=待人工复核（正确未冻结）。
- 评测补充（seed=42 已验证）：HAN 真异构困难场景 F1=0.810 > 结构社区 0.760 > GraphSAGE 朴素 0.304；资金链图 vs 改造前图 +0.314 F1。
- 回归风险：R7（get_fund_flow_digraph 纯新增）R8（_generate_gang_info 仅加字段 additive）R9（synthetic_data 受控参数，评测可复现）R10（build_graph 重置，防御性）。

## 下一步待做（按文档顺序）
- #7 Docker 完整后端跑 detect 验收 Stage1（无运行容器，阻塞，需用户起 Docker）
- #8 消融补"去反思/去门控"（需 orchestrator 暴露开关 flag）
- #14 文档对齐代码（LangGraph/6智能体/双路径Jaccard/HAN残实现/生产部署）
- #13 论文差异化+专利权利要求草稿
- #15 修复重接口 55% 报错
- #16 话术嵌入相似边双通道（可选）

## ArXiv 论文追踪 + 选题（用户请求，ArXiv追踪专家角色）
- 用 arxiv-watcher 技能脚本 + ArXiv API 检索，锁定 10 篇与项目组件贴合的论文，归档至 `E:\FraudLens\.workbuddy\memory\RESEARCH_LOG.md`。
- 交付报告 `E:\FraudLens\FraudLens_相关论文与选题.md`：论文映射表 + 7 条建议（P0 有向资金视图接 GNN / 置信度接 SHAP 归因；P1 伪装与不一致处理 / 时间衰减；P2 真实数据诚实标注 / 时序视图 / Related Work 定位）+ 4 论文选题（A 有向异构资金链团伙发现 推荐主标题）+ 2 专利选题（P1 反思闭环 / P2 置信度门控+回流闭环）+ 与公安大学"仅语义聚类话术"差异化要点。
- 关键洞察：主图无向是为零改接口，但 fund_flow 有向性未被 GNN 利用（对标 LineMVGNN），是有力差异化卖点；节点 hash 特征属 GraphConsis 的"特征不一致"问题。
- 报告文件 FraudLens_相关论文与选题.md 后已移入 docs/05_相关论文与选题.md（见下节文档整理）。

## 文档架构整理 + 清理（用户请求）
- 用户确认后删除 4 个无用文档：专家目录.md(112KB专家市场快照与项目无关)、FraudLens_新建项目模板填写内容.md(一次性)、支撑材料.md(旧版与代码不符)、test_upload.docx(测试残留)。token.txt 用户要求保留不动。
- 删前把支撑材料.md 历史信息(作者/理论基座/旧6阶段话术/旧技术栈描述)存档进 docs/04_论文进度.md 第七节。
- 建立 docs/ 文档中心架构（用户选"整理进 docs/"）：
  - 移入并重命名：01_项目背景与规范.md / 02_需求与开发流程.md / 05_相关论文与选题.md
  - 新建：INDEX.md(总索引) / 03_代码进度.md(进度表+已交付模块+实验结论+待办) / 04_论文进度.md(章节状态+差异化+专利+事实校准+历史存档)
  - 配图归入 docs/figures/
- readme.md 顶部加文档中心入口链接（readme 内"LangGraph 风格"等硬伤仍属 #14 待对齐）。保留根目录：readme.md、demo_*.docx、architecture.drawio。清理了临时 arxiv_fetch*.py。
- 根目录脚本审计与清理：start.bat/stop.bat 为 Docker 部署必需保留；test_analysis.py、test_docx_upload.py、request.json 为接口冒烟测试/示例保留；**删除** generate_doc.py（过时"支撑材料"Word生成器，含与代码不符表述）与 test_upload_api.py（引用已删的 test_upload.docx，已失效）。token.txt 按用户意愿保留（提醒有泄露风险，应进 .gitignore）。
- **确认 token.txt 已在 .gitignore（第68行）**，连同 test_*.py/request.json 一并忽略，无需改动。
- **软件架构师审计 + 架构文档落地**：
  - 派 Explore agent 彻底摸清 backend/ 真实结构（FastAPI+内嵌Flask-SQLAlchemy、Orchestrator 实际只调度 analyst+cluster、chat 为独立旁路、GNN HAN 真异构、fund_flow+account/perpetrator+回流闭环、客观置信度门控为真、降级链齐全）。
  - **关键纠偏**：代码里"反思闭环"是占位（单 if + `_adjust_strategy` 空操作 + 默认不触发），与记忆/专利 P1 主张不符 —— 已记入 MEMORY.md「重大架构风险」并写入 docs/06。
  - 新建 `docs/06_系统架构设计与发展规划.md`：现状审计(§1.1 真实能力/§1.2 技术债含专利硬伤) + 目标分层架构(§2) + 扩展/高可用设计 + Phase A-D 演进路线(§3) + 长期规划路线图(§4) + 风险决策(§5) + ADR(§6)。
  - 重画 `architecture.drawio` 为真实态 v1（旧图还在画 UMAP/HDBSCAN/5 Agent，已脱节），与 docs/06 配套。
  - 更新 `docs/INDEX.md` 登记 06 并标注 drawio 已同步。
  - 校正 `MEMORY.md` 第10行"反思闭环"表述 + 新增「重大架构风险」段（P0 专利硬伤/P0 Flask混用/P1 BGE双重/P2 巨型入口）。

- **架构→开发步骤拆解（docs/07）**：
  - 新建 `docs/07_开发任务拆解.md`：把 docs/06 的 Phase A–D 与 P0–P2 债拆成可验证任务，每个含目标/改动文件/拆解步骤/验证门/依赖/回归风险/工作量。
  - **agent 专项重点**：G0 决策门（A 真做闭环 / B 重定主张，二选一必须先定）→ A1.1–A1.5（真闭环）或 B0.1–B0.4（重定）→ B1.1–B1.4 Agent 注册表 → CHAT.1 chat 旁路明确化。
  - **新发现事实硬伤**：`backend/agents/orchestrator.py:1-2` docstring 谎称"基于 LangGraph 的工作流编排"，代码实为手写编排（与 MEMORY 一致）。此为 #14 文档对齐必修项，已写入 docs/07 §3.4 B0.4 与依赖图。
  - 任务看板登记 #17(G0) #18(A2) #19(A3) #30(AGENT-A,被17阻塞) #31(AGENT-B,被17阻塞) #26(B1) #22(B2) #20(B3) #27(C1) #29(C2) #23(D1) #24(D2) #28(CHAT.1)。
  - 更新 `docs/INDEX.md` 登记 07。

- **G0 决策落地：选 A + 采用 LangGraph（用户指令）**：
  - 用户决定走 A 路径，且把"反思闭环"用 **LangGraph StateGraph**（条件边回连）真做出来——顺带让 orchestrator.py:1-2 的"基于 LangGraph"注释由假变真，#14 的 LangGraph 假注释项自然消除（不再删注释，改为补节点/边说明）。
  - 同步修改的文档：`docs/06`（§1.1 多智能体编排改 LangGraph、§1.2 P0-1 标"已决策走A"、§3 Phase A 目标指明 LangGraph、新增 ADR-6）、`docs/07`（G0 表加决策结果、A1.1–A1.5 改写为 LangGraph StateGraph、§3.3 路径B标❌未选、§3.4 B0.4 改"补说明"、§5 依赖图去B分支）、`docs/02`（T1 改"已采用LangGraph"、T4 改"Phase A重构中"、G3 加[LangGraph真实现]）、`docs/04`（P1 主张改 LangGraph StateGraph、§6 事实表"基于LangGraph"改"已采用"）。
  - 同步 `MEMORY.md`：决策协同层改"基于 LangGraph StateGraph 编排"；不符点 LangGraph 改"已决策采用LangGraph真实现"；重大架构风险 P0-1 改"已决策走A，详见docs/06/07"。
  - 任务看板：#17 G0→completed；#31 AGENT-B→deleted；#30 AGENT-A 描述改为"LangGraph StateGraph 重构 + 真闭环"。
  - **下一步（待用户确认开工）**：从 #30(A1.1 LangGraph StateGraph) 起，按铁律"先讲改动点+回归风险"落地 orchestrator 重构；需先在 requirements 加 langgraph 依赖。

- **用户确认交接：拆解文档将交专门开发专家（2026-07-26 末）**：
  - 核验两处现状偏差并写入 `docs/07` §8 接手须知：① `backend/tests/` 目录不存在，验证门引用的 pytest 文件需随任务新建；② `backend/gnn/` 实际含 8+ 文件（gnn_model.py/tools.py/partitioner.py/monitor.py/inference_queue.py/community.py/deep_clustering.py/train_han.py），比文档枚举丰富，且 train_han.py 与 gang_detector._train_han 疑似重叠，A3/B2 前需盘清单。
  - 结论：拆解文档计划层完整、G0 已锁定、依赖图自洽，可交付开发专家；附 §8 坑位清单（tests 缺失、gnn 冗余、Docker 无运行容器、待用户拍板项）。

- **新建 `docs/08_待用户确认清单.md`（用户要求）**：列出拆解范围外仍需用户拍板的 3 项——D1 论文最终选题（04 已给推荐主标题）、D2 专利 P1 权利要求草稿（P2 已真实现可前置占位）、D3 #15 修 55% 报错接口（历史遗留未纳入 Phase）；每项含背景/需确认/影响/决策人/回填动作。同步登记进 INDEX.md。开发专家交接三件套齐备：06 架构 + 07 拆解(含§8接手须知) + 08 待确认清单。

- **用户拍板 D1/D2/D3，交接边界全闭合（2026-07-26 末）**：用户表示"看不懂了，往最好的方向来"，授权代为决策。
  - D1 ✅ 保留多智能体：最终主标题定为《基于多智能体编排与图神经网络的反诈资金链团伙发现》（LangGraph 多智能体为真实能力+资金链GNN 差异化，最契合架构）。回填 docs/04 §1/§3、docs/02 G2。
  - D2 ⏸ 专利暂缓：P1/P2 权利要求草稿暂不写，待代码主线稳定后。回填 docs/04 头部策略+§5、docs/02 G3（标暂缓）。
  - D3 ✅ 交开发专家且扩为**全量接口**排查修复：建任务看板 #31（实际ID复用原删掉的#31，非#32，文档已统一为#31），与 #30 并行。回填 docs/02 I1、docs/07 §8.5、docs/08（改"已处置记录"）。
  - **交接就绪**：三件套（06/07/08）口径一致、边界已清、无未决项，开发专家可直接开工（起点 #30 LangGraph 重构 + #31 接口全量修复并行）。

- **#30 LangGraph 反思闭环重构 — 已完成并单测通过（2026-07-26 开发专家开工）**：
  - 重建隔离 venv（`C:/Users/hd/.workbuddy/binaries/python/envs/default`，装 langgraph 1.2.9 + openai + fastapi + sklearn；torch 用惰性导入规避）。
  - `backend/agents/orchestrator.py` 重写为 LangGraph StateGraph：五节点(plan→preprocess→analyze→cluster→reflect) + `add_conditional_edges` 回连 analyze；真 `_adjust_strategy`（降 GNN 依赖/调聚类粒度）；retry_count 真实自增；`process()` 返回结构 100% 兼容旧契约；`ENABLE_REFLECTION_LOOP` 开关供 #8 消融；构造器新增 use_gnn/analyst/cluster/max_iter 可选参，保留 `OrchestratorAgent()` 无参调用。
  - **顺带修真 bug（属 #31 范围）**：`cluster_agent.py` GNN 分支 `detect()` 返回无顶层 `total_gangs` → orchestrator 第112/147行 `gang_result["total_gangs"]` 必抛 KeyError（analyze 接口高失败根因）。已补 `result['total_gangs']`；并把 `from gnn import GangDetector` 改为惰性导入（无 torch 环境可 import/单测）。
  - 单测 `backend/tests/test_orchestrator_loop.py`（3 例全过）：闭环触发+_adjust生效+结果改变+契约不变；max_iter=0 退化为单次；真实 agent(use_gnn=False)离线跑通 LangGraph 图。
  - **#31 现状核查**：Docker 守护进程在线；但当前运行的是**其他项目**容器（vul-db 的 mysql/phpmyadmin、fastapi_ai_interview 的 nginx，后者 Restarting=crash-loop），**FraudLens 的 compose 栈未在运行**（无 fraudlens backend/mysql/redis）。下一步：用 start.bat / `docker compose up -d` 拉起 FraudLens 栈（可能需网络拉镜像+拷 BGE 模型），再跑全量接口冒烟（test_analysis.py/test_docx_upload.py）并修复失败项。

- **#31 接口全量修复 — 本地验证完成（本日收尾）**：
  - 起 FraudLens 全栈成功（mysql/redis/backend/nginx 均 Healthy；基础镜像 mysql:8.0/redis:7-alpine/nginx:alpine 本地齐备，离线可起；backend 镜像 `fraudlens-backend:latest` 本地已存在）。
  - **两处真 bug 已修并验证**：
    1. `/agent-analyze` 500 `No module named 'agents.chief_agent'`：根因**镜像源码比主机源码旧**——镜像 `routes/system.py` 仍引用已移除的 `ChiefAgent`。修复：新增 `docker-compose.dev.yml` 开发验证覆盖，把当前主机源码整体挂进容器（agents/routes/core/tools/database/gnn/rag/utils/memory/data + 入口文件），`build: null` 不重建、`entrypoint` 启动即 `pip install langgraph`。
    2. `/agent-analyze` 聊天格式 `{role,content}` 消息被静默丢弃（total_cases=0）：`routes/system.py` 对 dict 消息原样透传、未提取 content→description。已改为提取 `content→description`。
  - **`backend/requirements.txt` 补 `langgraph>=0.2.0`**（#30 引入的依赖此前缺失，mirrors 用清华源）——下次 `start.bat` 全量重建即含；此前后台 `docker compose build backend`（任务 xaPrEd）因 requirements 缺 langgraph 会烤出缺包镜像，已用 dev 覆盖规避。
  - **验证结果**：`/health` 200；`/api/auth/login` 200；`/api/extract-text` 200（docx 正确）；`/agent-analyze` 单消息→200(total_cases=1)、双消息→200(total_cases=2,total_gangs=1) → **#30 LangGraph 流水线在部署栈端到端跑通**。
  - 交付文档：`docs/09_变更记录.md`（#C1–#C4，每改必记，满足用户"改动可追溯"要求）；`docker-compose.dev.yml`；`backend/tests/test_agent_multi.py`（多消息冒烟）。
  - **待跟进（非今日范围）**：① 生产镜像全量重建（彻底解决 stale，已含 langgraph）；② 其余接口（cases/gangs/dashboard/alerts…）尚未逐一冒烟；③ 单聊天消息场景下 avg_risk_score=20 偏低，团伙 risk_level=LOW，属规则聚类特征，非阻塞。
  - **铁律遵守**：每步改前讲改动点+回归风险；所有改动已记入 `docs/09_变更记录.md`。

- **数据层选型决策落档（ADR-7，软件架构师角色）**：
  - 用户问"数据库要用关系型吗" → 明确结论：**保留 MySQL 作主关系库（单一事实源）**，但采用多模存储，不把图/向量/大文件硬塞进 MySQL。
  - 写入 `docs/06` §6 ADR-7：MySQL 存结构化+审计/冻卡决策；图与向量为内存/服务派生视图不落库（graph_builder 内存构建、BGE 收敛单一 Embedding 服务并入 HAN 节点特征）；Redis 扛缓存/WS进度；本地 minio 扛文件；全部本地部署满足"数据不出域"，不引入云原生图库（破合规/增运维），内存图足够公安实战规模；并明确 P0-2(Flask混用)与"用不用关系型"正交。
  - 同步配图：多模存储分层 SVG（已 inline 展示给用户）。

- **ADR-7 → 开发流程拆解（代码开发流程专家角色）**：
  - 用户要求把 ADR-7（数据层选型）也拆成可验证开发步骤写进 docs。先核验代码现状：① minio 完全未接入（compose 无服务、代码零引用）→ 净新增；② 仅 `OperationLog` 表、`gang_detector.py` 无 `db.session` → 冻卡决策不落库，合规缺口，净新增；③ `graph_builder` 仅 `torch.save`/`pickle.dump` 存**模型权重**、图内存构建无 `nx.read/write` → "图不落库"已满足，仅加验证。
  - 写入 `docs/07`：新增 §2 **A4（ADR-7）数据层选型落地（多模存储）**，含 A4.1(minio 接入,2d) / A4.2(冻卡决策持久化,1.5d) / A4.3(图不落库验证,0.5d) / A4.4(Redis WS 确认,0.5d)，每个含目标/改动文件/拆解步骤/验证门/依赖/回归风险/工作量；同步更新 §5 依赖图、§7 对接、§8.6 接手须知(列三项缺口+约束)；总工作量约 4.5 天。
  - 任务看板建对应条目 **#32(A4.1) #33(A4.2) #34(A4.3) #35(A4.4)**，遵循 docs/07 §0 约定。

- **开发文档收尾（接手包定稿，代码开发流程专家）**：
  - 用户明确分工：本专家只整开发文档，落地交另一专家。定稿动作：`docs/07` §0 编号补 A4、§8.1 加接手阅读顺序；`docs/INDEX.md` 07 行补 A4、加「接手三件套（06→07→08，改动记 09）」提示。
  - 至此开发接手包齐备：06 架构(含 ADR-7 数据层选型) / 07 拆解(含 A4 与 §8 接手须知) / 08 边界(已处置) / 09 变更记录；看板 #17–#35 与文档一一对应。本专家不再碰代码，等用户切专家落地。

- **#31 静态审计收尾 + #8 消融（开发专家续做，2026-07-26 末）**：
  - Docker 守护进程本会话不可达（连不上 docker API），故 live 全栈冒烟暂跑不了；改为离线推进 #31 与 #8。
  - **路由静态审计（派子代理全路由核对）**：发现并修复 **cases.py 用 logger 未导入** 的确定 500 根因——雷达接口(DB/Redis 抖动时 except 块 NameError→500，与"55%报错"背景吻合)；并修复 reports.py/merges.py 把"资源不存在"的 ValueError 误报 500（改 404/400）。
  - **#8 消融开关落地**：新增 `backend/gnn/ablation.py`(torch-free) 含 `compute_gate_decision`(去门控时一律冻结) + `evaluate_gating_ablation`(量化误冻率)；`gang_detector` 加 `enable_gating` 参数 + env `ENABLE_CONFIDENCE_GATING`，门控决策改调该函数（阈值 0.5 不变）；`eval_framework.run_all` 加 `ablation_flags=['no_reflection'|'no_gating']` 接入两消融（去反思复用 #30 的 ENABLE_REFLECTION_LOOP）。
  - **测试与验证（离线可跑）**：`backend/tests/test_ablation.py`(torch-free) 全过；新建 `backend/tests/test_routes_fullstack.py` 全量接口冒烟(显式断言三处回归修复)；补 `backend/tests/__init__.py`；全部改动 `py_compile` 通过。
  - **待用户做的下一步**：起 Docker（`start.bat` 或 `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d`）后跑 `python backend/tests/test_routes_fullstack.py` 做 live 验收 #31；跑 `python backend/gnn/eval_framework.py --seed 42` 出消融数字。
  - 变更记入 `docs/09_变更记录.md` #C5(路由500修复) / #C6(#8消融)。待办 #8 已落地（逻辑离线验证，full eval 需 Docker/torch）。

- **#31 live 验收 + #8 完整评测（Docker 已起，2026-07-26 晚）**：
  - 用户起 Docker 后，用 `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --force-recreate backend` 重起后端（挂载当前宿主源码 + 补 langgraph），使 #C5/#C6 修复在容器内生效。
  - **#31 全量接口 live 冒烟（test_routes_fullstack.py）26/26 全部通过 ✅**：核心接口(案件列表/详情/统计/搜索、团伙列表/详情/雷达、仪表盘/趋势、预警、会话、合并待处理、复核待处理、图统计、/agent-analyze)全 200；三处回归点全部生效——雷达-案件/雷达-团伙 200(原必 500 的 logger 缺失已修)、reports 不存在资源 404、merges 不存在对 400；中文团伙 id URL 编码修复后详情/雷达亦 200。
  - **新根因 reportlab 缺失（#C7）**：live 初跑 reports 端点仍 500，真实根因 `No module named 'reportlab'`（PDF 依赖漏装，requirements 仅含 python-docx）。容器内 `pip install reportlab` 后端点对不存在资源正确 404（`Gang ... not found` ValueError→404），证明 #C5 的 404 逻辑本身正确、被依赖缺失掩盖。已将 `reportlab>=4.0.0` 补入 `backend/requirements.txt`，下次 start.bat 全量重建即含。
  - **#8 消融接入完整评测 CLI（#C8）**：`eval_framework.__main__` 此前未暴露 `ablation_flags`，`python eval_framework.py` 不跑消融；新增 `--ablation nargs="*"`（no_reflection/no_gating）传两次 run_all(理想+困难)。后台跑 `python -u eval_framework.py --seed 42 --ablation no_reflection no_gating`（容器内 torch 可用），结果写 `backend/gnn/eval_results.json`，task EaXs8X 进行中。
  - **结论**：#31 接口全量修复已 live 闭环（26/26）；#8 消融开关逻辑 + CLI 就绪，完整评测已跑通（gating 消融不再跳过），可关 #8。下一步按 docs/07 推进 **A4（数据层选型落地：minio 冻卡决策持久化/图不落库验证）** #32–#35。
  - **#8 完整评测关键数字（seed=42, 40 cases/5 gangs, 2026-07-26 晚跑通）**：
    - 真异构 HAN 完胜：理想 NMI/ARI/F1=1.0/1.0/1.0；困难=0.926/0.879/0.901（远超图系统 0.760、塌缩 GraphSAGE 0.304/#簇=1 → 印证论文差异化卖点）。
    - 资金链增益：理想 F1 +0.31、困难 F1 +0.073。
    - 去门控消融：干净合成集检测纯度高（HAN 完美）无假阳性，门控保守（真团伙亦转人工复核），误冻 0/0；门控降误冻量化价值由 test_ablation.py 构造低置信假阳性场景展示（离线单测已验证）。
    - 注：Semantic 基线为 TF-IDF 代理（BGE 权重未下载）；图系统困难场景降至 0.760（跨团伙账户共享干扰）。结果落 backend/gnn/eval_results.json。

- **A4 数据层选型落地（#C9/#C10/#C11，2026-07-26 深夜）**：
  - 用户"继续落地"+ Docker 已起。按计划 §5 推进 A4：本轮做 **A4.2 冻卡持久化 + A4.3 图不落库验证 + A4.4 Redis WS 确认**；A4.1 minio（最重，需加 compose 服务+改上传/导出接口）留作下一步。
  - **A4.2 冻卡决策持久化（#C9，NET NEW）**：`models.py` 新增 `FreezeDecision` 表(与 OperationLog 互补，记"决策事实")；`crud.py` 新增 `persist_freeze_decisions`(落库+互补写 OperationLog，独立 try 不影响主流程，幂等建表 checkfirst)+ `get_freeze_decisions`(复核台查询)；`gangs.py` 的 `/detect/gnn` 研判后落库 + 新增 `GET /api/gangs/freeze-decisions`(置于 `/{gang_id}` 前避免路由冲突)；`system.py` 的 `/agent-analyze` 研判产出后落库。
    - **验证**：`pytest tests/test_freeze_decision.py` 2 测试全过(落库+查询+互补日志、按 gate 筛选)；端到端 live `POST /detect/gnn`(GNN,15 cases)→`GET /freeze-decisions` 返回 2 条落库(gate=待人工复核,conf 0.388/0.137<0.5 门控正确)。**完整链路实锤 working**。
  - **A4.3 图不落库验证（#C10）⚠️ 发现重大偏差**：原以为图不落库（§8.6 误判"已满足"），实测 `graph_builder.py` **已真实读写 MySQL 图**——`build_graph` line 58-71 优先从 Redis/MySQL(`GraphNode`/`GraphEdge` 表 models.py:85-112)加载旧图并 return，line 234-237 build 后落库。即**图当前确实持久化，与 ADR-7 字面"图不持久化"冲突**，且 use_db/use_cache 开启时有 stale 风险(cases 变但 DB 有旧图返回旧图)。
    - **未擅自剥离**（会破坏前端 network-data + 跨 A2 + 铁律稳妥优先），仅写 `test_graph_ephemeral.py` 锁定"从源重建确定性"防 stale 基石(2 测试全过：重建一致 + 无图文件序列化)。
    - **⚠️ 待用户拍板**：保留图快照语义并修订 ADR-7(推荐) / 剥离持久化 / 维持现状标注。属真实架构决策，需 owner 定。
  - **A4.4 Redis WS 确认（#C11）**：确认 `deps.py:273` 进度 publish 到 Redis `progress:{session_id}`，`system.py:191-234` WS 从 Redis pubsub 订阅——WS 进度链路已走 Redis，满足 ADR-7 无状态多副本进度不丢，零代码仅记录。
  - 变更记入 `docs/09_变更记录.md` #C9(A4.2) / #C10(A4.3 偏差) / #C11(A4.4)。临时 e2e 脚本已清理，目录干净。
  - **下一步**：用户拍板 A4.3 图持久化走向后，做 **A4.1 minio 接入**(加 compose 服务+`core/object_store.py`+改上传/导出接口写 minio 而非 MySQL/裸盘)。

- **A4.3 推荐方案①落地（#C12，2026-07-26 深夜，用户"继续按照推荐方案"）**：
  - 用户确认按推荐方案①：保留图快照语义，修订 ADR-7；detect 强制从源重建(use_db=False) + 保留 save 供前端展示最近一次图。
  - **改动**：`gang_detector.py` `__init__` 改 `FraudGraphBuilder(use_db=False, use_cache=False)`(研判永远从源重建，绕过 build_graph:58-71 的 load 旧图分支)；新增 `persist_graph_snapshot()`——研判后**先 GraphEdge.delete 再 GraphNode.delete 并 commit**(修正外键顺序 bug)再写回 DB+Redis 干净快照供前端展示；`test_graph_ephemeral.py` 加 `test_detect_defaults_to_ephemeral` 锁 `use_db is False`。
  - **踩坑**：`persist_graph_snapshot` 初版先 `GraphNode.delete` 因外键约束(`graph_edges.source_id` 引用 `graph_nodes.node_id`)失败、异常被 except 吞、幽灵节点没清；改子表先于父表 delete 后修复。验证用"注入幽灵节点 GHOST_STALE_NODE(模拟历史旧图)→detect→幽灵被 truncate 清除"的端到端脚本，**STALE_GUARD_RESULT=PASS**(detect 前 GraphNode=70 含幽灵，detect 后=69 仅真实案件节点，case 节点=15，无幽灵)。
  - **验证**：`pytest tests/test_graph_ephemeral.py` 3 项全过；端到端 stale 防护 PASS。
  - **文档**：`docs/06` ADR-7 第 178 行后补修订说明(图非真值但允许前端快照暂存+修正 stale)；`docs/09` #C10 状态改"已落地" + 新增 #C12 完整记录。
  - **A4 整体进度**：A4.2(#C9) + A4.3(#C12) + A4.4(#C11) 全部完成并 live 验证；仅剩 **A4.1 minio 接入**(最重，需加 compose 服务 + `core/object_store.py` + 改上传/导出接口)。

  - **A4.1 minio 接入（#C13，2026-07-26 深夜，用户"继续"）**：
    - **改动**：`requirements.txt` 加 `minio>=7.2.0`；`docker-compose.yml` 加 `minio` 服务(minio/minio, console 9001, 桶 fraudlens, minio-data 卷) + backend `MINIO_*` env；新建 `core/object_store.py`(put/get/presigned/delete/is_enabled，**优雅降级**——minio 不可用退回本地绝不 500，is_enabled 首次触发懒连接自动建桶)；`files.py` 的 `/extract-text`、`/ocr` 存原始上传件到 minio(`raw/{ts}_{md5}_{filename}`)返回 `object_url=/api/object/{key}`；`reports.py` 导出报告存 minio(`reports/{filename}`)返回 `object_url=/api/object/{key}`(minio 不可用时退回本地 /api/reports/download)；`files.py` 新增 `GET /api/object/{key:path}` **后端代理下载端点**(鉴权后从 minio get_object 流式返回，数据不出域，前端不直连 minio)；新建 `test_object_store.py`。
    - **踩坑**：① presigned URL 替换 host(minio:9000→localhost:9000)致 **SignatureDoesNotMatch(403)**——签名按 minio:9000 算，host 改即失效；改为 presigned 保持原生 + 外部统一走 `/api/object` 后端代理。② dev 覆盖 `build:null` 复用旧镜像，force-recreate 重建容器丢失 pip 装的 minio/reportlab——容器内补装；根治靠 start.bat 全量重建(requirements 已含)。
    - **验证**：`pytest test_object_store.py` **3/3 通过**(真连 minio)；端到端 live：① `POST /extract-text` 上传原件存 minio 成功返回 /api/object；② 宿主机 `GET /api/object/{key}`(带 token) **HTTP 200** 下载内容一致；③ `GET /api/reports/case/CASE-2024-0012` 导出 PDF 存 minio 返回 /api/object 成功。
    - **A4 全部完成**：A4.1(#C13)+A4.2(#C9)+A4.3(#C12)+A4.4(#C11) 均 live 验证通过。Phase A 数据层选型落地收官。
    - **下一步**：按计划推进 Phase B(Agent 注册表 B1.1–B1.4 / 可插拔流水线) 或 C(数据源适配器)，或做 #14 文档对齐(论文按真实版写：LangGraph 真闭环 + 真异构 HAN + 资金链 + 客观门控 + minio 存储)。用户定方向。
