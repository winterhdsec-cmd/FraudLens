# 2026-09-17（答辩日）

## 答辩念稿定稿（纯念稿版）

文件：`答辩PPT/慧眼识诈——基于多智能体与图神经网络的智能研判系统_答辩逐字稿.md`
旧版（双标记「念板上/人话」）备份在 `_ppt_work/_old_答辩逐字稿_双标记版.md`。

**用户明确要求（本轮）**：① 不要复杂结构，直接给"该念什么"；② 全程照着念，所以衔接词必须顺；③ P7 加一句引导评委看申报书哪一页；④ 团队介绍里加教师简介。

**定稿规格**
- 结构：`## Pn · 章节 · N 秒` + 〔动作提示，不念〕 + 一行 `> 念词`（照念）。
- **854 口播音节 = 4 分 44 秒**（@180 字/分钟；@170 为 5 分 01 秒）。距 5 分钟留 16 秒缓冲。
- 每页秒数：P1 21 / P2 28 / P3 30 / P4 27 / P5 28 / P6 22 / P7 48 / P8 16 / P9 30 / P10 29 / P11 6。
- 中途对时表：念完 P3 应到 1:19、P6 到 2:35、P9 到 4:09。
- 减负位（依次）：P8 不念两个数字 −8 秒 / P7 不念申报书页码引导 −10 秒 / P10 不念"先证伪再主张" −12 秒。
- 必背口径 6 条（B1–B6）保留在文末，含 B5「本地优先，不说数据不出域」。

**申报书页码（已用 Word COM 导出最新 docx 核实，36 页，与 09-14 版一致）**
- **P23「七、现有成果」** ← P7 证据页引导目标（成果原则：看得见/可运行/有立项/有论文/有代码/有应用）
- **P34–36** 三份成果应用证明（图 11/12/13）
- P16 图 8 六种场景效果对比（表 1）；P17「（五）局限性的如实说明」；P4–P5 团队与指导教师介绍。

**P9 教师简介（取自申报书 P4-P5）**：吴燕波老师系信息技术系副教授、华中科技大学硕士，方向为网络安全/电子数据取证/警务信息化，学院电子取证重点实验室骨干，**全程深度参与项目研发**（非挂名）。

## 时长实测口径（重要，别再算错）
- **5 分钟上限 ≈ 870–880 口播音节**，不是 1000 字。
- 音节 = 汉字数 + 非汉字（数字/字母）×1.5；按 180 字/分钟折算秒数。
- 每页预算 ≈ 78 音节；P7 证据页给 48 秒（全场最高），P11 结尾 6 秒。

## 踩过的坑
- **正则分隔符与内容字符冲突**：用 `^## (P\d+) · ([^·\n]+) · (\d+) 秒$` 解析标题时，`[^·]` 遇到标题里的全角「·」（如「核心技术·创新一」）直接失配，整页被跳过 → 统计出 543 音节的假值。改为 `^## (P\d+)` + 非贪婪去秒数后缀才准。**结论：统计脚本的锚点只能用页号，章节名一律不要参与匹配。**
- 手写压缩易被写入校验拦下 → 一律用脚本做整段替换，并让脚本回写每页秒数 / 总时长 / 减负位秒数，避免"表里表外对不上"。

## 待办
- 用户仍未回：是否给 `backend/tools/vision.py` 补云端开关门禁（现在开关管不住图片上传那条路）。
- 立项名不一致（申报书「A4 反诈画像系统」vs 项目名「慧眼识诈」）——已备口头答法，无需改板。

---

## 第二轮改稿（用户逐条念了一遍，提 8 条意见）→ 实测 8 分 46 秒

用户听读后提出的 8 条，已全部落进稿子：
1. **P2**：三个卡点各加一句解释（数据质量→账户缺失/号码打码/格式各异；人工比对→一案被拆几十条线索、凭经验关联；话术节奏→两小时换版本、词库滞后）。
2. **P3**：说明"中间四张是系统截图"并逐张讲用途；四个环节各加一句解释。
3. **P4**：明确"诈骗案主要看两条路"——先资金链相似度，再话术相似度。
4. **P5**：改成从民警办案规矩切入的逻辑（办案要严谨→证据不够不出结论；民警会发现机器聚错、会要求重判→系统把复核做成自动）。
5. **P6**：解释"三类产品都够不着"是什么意思；预警平台＝事前拦截资产流失；风控＝服务银行、看交易风险；通用图谱＝需专人驻场，县区没这编制。并点明用户是县区反诈民警。
6. **P7**：删掉"比我们自己搭的两种经典方法都好""右下角写了诚实标注"这类废话（用户原话：这都是废话）。
7. **P8**：把两个数字讲出因果（省警力＝串并从天压到分钟；守民财＝串并越早冻结越及时）。
8. **P9**：重写为两条线——本校信息安全专业四人（韩冬/李家兴/张高瑞/黎意博）+ 校外三人（武汉理工刘亦天、长江大学朱明菲、中南财经政法朱则希），各自角色与院校，强调"不只我们学校"。
9. **P10**：右下角"先证伪再主张"改写成人话——"早期实验有个特征用错了，之前的结论全不作数，我们没绕过去，全部推翻、七个人重跑一遍"。

**关键板上事实（本轮核对最新 PPT，11 页）**：P3 中间四张截图 = **关系图谱 · 团伙画像 · 资金流向 · 研判报告**；四个环节 = 数据接入/要素解析/团伙发现/成果输出。P9 板上七人四线：湖北警官学院信息安全 4 人（24 级）+ 武汉理工（电子信息 24）/长江大学（商务英语 23）/中南财经政法（法学 24）。P5 右栏三卡＝专职分工/反思回炉/本地运行。

**时长硬账（最重要）**：
- 本版全文 **1578 音节 = 8 分 46 秒**（@180）。
- **把 9 处解释全删光仍有 1014 音节 = 5 分 38 秒** —— 因为 P9/P10 重写本身是净增。
- 5 分钟上限 ≈870 音节。→ **用户"每处加解释"的要求与 5 分钟硬约束数学上冲突**，必须由用户取舍（已备删减清单：9 处依次删各省 9–29 秒，最后 P6 整页只提一句 −43 秒）。
- 旧文档《模拟答辩与风险清单》写的是**8 分钟路演 + 5 分钟问答**，与用户口述的 5 分钟矛盾 —— 已在回复中请用户确认赛制。

---

## 定稿：压到 5 分钟（用户拍板"把它压到5分钟"）

**结果：877 口播音节 = 4 分 52 秒**（@180；@170 = 5 分 09 秒）。对时点：P3 → 1:27 ／ P6 → 2:48 ／ P9 → 4:16 ／ P11 → 4:52。

**压缩取舍（记录清楚，免得以后又反复）**：
- **保住的**：P2 三个卡点各一句解释；P3 四张截图逐张说用途 + 四环节各一短语；P4"主要看两条路——资金链相似度、话术相似度"；P5"办案要严谨→证据不够不出结论""民警发现聚错了会要求重判→系统做成自动复核"；P6"事前拦截资产流失 / 服务银行 / 需专人驻场"+县区民警定位；P7 成果条 + 2 家投用 1 家调研 + 申报书第 23 页引导（**删掉"比自建两种经典方法都好""右下角诚实标注"两句废话**）；P9 本校四人（含"为信息技术系在全国信息安全竞赛、挑战杯、电子取证大赛拿过成绩"）+ 校外三人及各自职责；P10 特征缺陷用错→全部推翻重跑的**人话版**。
- **压掉的**：P3 四环节原有的完整解释句（改为"接入把材料对齐、解析拆要素、发现找团伙并复核、输出出证据链和报告"）；P6 三类产品的完整解释句（压成三个短句）；P7 四张图逐张说明（由成果条带过）；P8 两条因果（合成一句）；P9 每位成员原本 6–8 字的角色描述压成 2–3 字。
- 减负位（真超时）：P6 只念定位那半句（−15 秒）、P8 只念"惠及谁"那一行（−13 秒）。

**方法论**：时长硬约束下，"每处都加解释"不可能全保 —— 正确做法是**先按用户要求把内容写全（8 分 46 秒），再用脚本逐句量出哪句值多少秒，按"评委最在意什么"排序取舍**，而不是凭感觉删。本轮全程用脚本回写每页秒数、总时长、对时表（对时表曾把音节数当秒数输出，已修）。

---

## 老师临场要求 → 第三轮重排权重（最终定稿）

**老师指示（上午）**：时间紧，主要谈**项目背景、项目详情、创新点解决了什么痛点**；**弱化项目成员、指导老师、公司/团队的展示**。

**定稿：867 口播音节 = 4 分 48 秒**，核心四页（P2-P5）**175 秒 = 61%**。对时点：P3 → 1:39 ／ P6 → 3:32 ／ P9 → 4:18 ／ P11 → 4:49。

| 页 | 秒 | 定位 |
|---|---|---|
| P2 项目背景 | 47 | **加重**：三个痛点各带解释 + 资金流切入依据 |
| P3 项目概述 | 34 | **加重**：四张截图逐张说用途 + 四环节 |
| P4 创新一 | 39 | **加重**：明确"正对着第二个痛点" |
| P5 创新二三四 | 55 | **最重**：逐条对应痛点（含痛点①的数据柔性适配） |
| P6 市场定位 | 19 | 弱化 |
| P7 项目应用 | 31 | 保留证据 + 申报书第 23 页 |
| P8 社会价值 | 6 | 极弱化（只念一行） |
| P9 团队介绍 | 10 | **极弱化**：一句"七个同学来自四所学校四个专业，指导教师吴燕波老师全程参与" |
| P10 个人成长 | 25 | 保留"特征用错→全部推翻重跑" |

**新叙事骨架：创新 ↔ 痛点一一对应（老师要看的就是这个）**
- 痛点① 数据质量跟不上 → **数据柔性适配**（解析遇缺失字段自动留空不报错，靠别的证据通道补位）
- 痛点② 人工比对跟不上（案子被拆散）→ **创新一 双通道共识**（资金链相似度 + 话术相似度，两路一致才作锚点）
- 痛点③ 话术与节奏跟不上 → **创新三 增量接入**（新案直接挂老团伙，无需全库重算）
- 另两条不直接对痛点、但对警务规矩：**创新二 拒绝输出**（办案要严谨→证据不足不下结论）、**创新四 反思闭环**（民警发现聚错会要求重判→系统把复核做成自动）

---

## 第四轮：P8 四价值各补一句短话（用户要求）+ 用户同期手改

**定稿：879 口播音节 = 4 分 53 秒**，核心四页（P2-P5）480 音节 = 55%（因用户删短 P5、加长 P9 而下降）。

P8 现为（23 秒，原 6 秒）：
> 一句话——惠及谁？省警力、守民财、兴产业、立规范。**省警力**，机器替民警翻流水；**守民财**，串并快、钱更好追；**兴产业**，轻量本地部署、降低基层门槛；**立规范**，本地优先加出向脱敏、全程留痕。

**用户本轮同时手改了 5 处（全部保留，不得覆盖）**：
1. P5 删掉"先说第一个痛点数据质量…数据柔性适配"整句；"系统"→"**智能体**把复核做成自动的"。
2. P6 加"专门"（"专门县区反诈民警"）。
3. P7 改"详细材料**和论文全文**在申报书第 23 页"。
4. P9 加回三所校外院校名："我们还邀请了武汉理工大学，中南财经政法大学，以及长江大学高校的同学参与项目"（P9 变 22 秒）。
5. P10 删掉"最想说的一件事：早期实验有个特征用错了…"整句（P10 变 15 秒）。

**⚠️ 待确认的逻辑缺口**：用户删掉 P5 那句后，**三个痛点里的痛点①（数据质量）在口播里没有对应的创新**了。PPT P5 左栏第三卡仍是"数据柔性适配·脏数据不报错"。若评委追问"数据质量怎么解决"，需口头补答——已在回复中提醒用户。

**操作规范（重要）**：用户会在两次会话之间直接手改这份 md。**每次改动前必须先读文件确认当前文本，且只用片段级替换、不用整文件重写**；本轮就有 4 处替换因文本已被用户改动而失配（好在脚本用了"找不到就跳过+告警"而非 assert 中断）。

**痛点①的缺口 → 用户拍板用"口头备答"（不改主稿）**：口径表新增第 2 条「**数据质量跟不上，你们怎么解决？**（口播里没说，被问才答）」→ 答法："解析遇到缺失字段自动留空、不报错，靠别的证据通道补位——不等着数据变干净，而是让系统适应脏数据"，并指明**版面对应 P5 左栏第三张卡"数据柔性适配"**，可指着卡答。主稿未动，仍 879 音节 = 4 分 53 秒。

---

## 上午模拟答辩通过（老师说"非常好，PPT 加稿子"）→ 转准备下午正式答辩问辩

**用户诉求**：怕被问"企业在你们项目里到底做了什么"。用户说"我不知道的点"就是企业应用成效。

**三份成果应用证明原件实读（`C:\Users\hd\Desktop\学习生涯\挑战杯\应用成效\`，均为 2026-04 盖章，项目名统一写"灵犀智瞳——电信诈骗犯罪 AI 画像与智能研判平台"）**：
1. **千景绘科技集团有限公司**（武汉·武昌万达尊 B 座 2001）｜测绘、信息化、大数据时空智能｜"已在本单位**多项信息化项目**中投入实际应用"，能力点是**多源数据智能分析与可视化呈现** → **已投用**
2. **武汉市新兴亿佳网络科技有限公司**（武汉·电脑大世界 612）｜安防产品、监控设备、系统集成｜"已在**多项安防集成项目**中投入实际应用，证据留存规范"，能力点是**多源数据汇聚与智能分析** → **已投用**
3. **山东世纪恒安信息技术有限公司**（济南·天桥区小清河北路黄岗工业园三区 15 号）｜网络安全与公共安全｜原文"**初步的技术调研与方案研讨**""**在模拟业务场景中**展现出分析关联能力" → **未落地，仅调研**

**关键结论（对外口径）**：企业里用的是**通用能力**（多源数据汇聚＋关联分析＋可视化＋可信留痕），**不是反诈案件研判** —— 企业没有反诈数据。⚠️ 红线：绝不能说"在企业里做了反诈研判"；绝不可编造"效率提升 X%"（证明里只有定性描述）。

**产出**：`答辩PPT/答辩问辩准备_慧眼识诈_下午正式答辩.md` —— 六节共 24 问（企业应用 6 问 / 技术 7 问 / 商业 3 问 / 团队 3 问 / 将军题 5 问 / 兜底话术），全部答法基于原文与代码事实，标注了红线与"主动承认加分"点。

## 数据来源口径（核实自申报书 P25/P28/P31 原文，答辩与论文必须一致）

**用户问"老师问数据是不是都是模拟的怎么办" → 答：不是"都"模拟，要分三层说。**

| 数据 | 性质 | 关键数字 |
|---|---|---|
| 自建六种警情场景（P0 干净／P1 轻噪／P2 重噪／P3 200案／P4／P5 极端） | **模拟**（主体实验） | 45 组实验 = 15 seeds × cross∈{0.0,0.2,0.4}；HAN 总体 F1 0.918 |
| AMLSim（反洗钱公开基准） | **模拟**（合成数据集，勿说成真实案件） | 小样本 12环×12账户：Louvain 1.0／KMeans 0.11／GraphSAGE 0.14；大规模 43,614 账户：**所有方法 F1≈0.002** |
| **Elliptic Bitcoin 交易图** | **真实**（203,769 笔真实链上交易） | 端到端盲扫**全部失效**，HAN F1_illicit=0.016 |

**申报书原文结论**："瓶颈在**任务设定**而非编码器" → 据此改用**扩线（refinement）设定**：给定少数锚点账户，在 k 跳嫌疑子图内恢复同伙团伙（真实 Elliptic 图上以 10 个 illicit 锚点取 2 跳，5 次独立试验）。这也贴合民警真实工作流（手上总有已知嫌疑人）。

**答辩答法（三段，已写入问辩文档 Q8）**：①承认主体是模拟，原因是《个保法》《数安法》限制、学生团队无权限；②亮出外部验证含**真实** Elliptic 链上数据；③**主动交边界**——真实图上所有方法都失效（不只我们），这是"量化了边界"的学术贡献，比刷高 F1 更有价值。
⚠️ 红线：**AMLSim 是模拟数据集，不能说成"真实案件数据"**；真实的那个是 Elliptic。

---

## 【代码/数据修复】"案件-团伙关联证据"模块看着空白 → 已修

**现象**：`DetailsView.vue` 第 269–294 行「案件-团伙关联证据」卡片内容单薄/像空白。

**排查链路（全部实测，非推测）**：
1. 前端 `currentGangRelationEvidence`（DetailsView.vue:520）从 `currentGang.related_cases / relation_reasons / matched_entities_map` 取数 → 数据源是 `useFraudLens.js:1575` 规范化 + `api.js:263 fetchGangs` → `GET /api/gangs` → `crud.get_all_gangs` → `_build_gang_dict`。**链路本身是通的。**
2. DB 实测：`gangs` 49 行、`gang_case_relations` 230 行，**case_id 与 cases 表 100% 匹配（0 失配）**，关联数为 0 的团伙 = **0 个**。→ 不是"没有数据"。
3. 真正的根因：**`reason` 字段 230 条全为空、`relation_type` 一律 `gnn_cluster`、`matched_entities` 153 条为空**。即该表是早期 seed/导入时写入的，`_upsert_gang_case_relations` 里的 `_auto_relation_reason` 兜底**从未在历史数据上生效过** → 前端每张卡片只剩「案件号 + GNN聚类标签 + 灰色兜底字『GNN 聚类关联』」，看着就是空的。

**修复动作**（脚本 `_fix_gang_rel_reason.py`，可重跑）：
- 先全表备份 → `_ppt_work/_bak_gang_case_relations_0917_1532.csv`（230 行）。
- 按**真实字段**推导理由（不编造）：① 案件 `keywords` ∩ 团伙 `fingerprint` → "话术特征命中：X、Y"；② 案件 `scam_type` ⊆ 团伙 `script_type` → "话术类型一致：X"；③ `similarity` → "语义聚类相似度 0.85"；④ 加 "归入团伙「名称」"。
- 回填 230 条，复查 **reason 空值 0 条**；类型分布 `similar_text=221 / gnn_cluster=9`。
- 验证 `_build_gang_dict` 输出：`relation_reasons` 已 5–6 项，样例 reason = `话术特征命中：冒充公检法；归入团伙「冒充公检法犯罪团伙4号」；语义聚类相似度 0.85`。

**遗留（诚实，未动）**：
1. **`matched_entities` 仍为空** —— 因为 `cases.extracted_entities` 在库里**全是空 `{}`**（要素抽取结果没落库），没有真实实体可填，**不编造账号**。这是比本 bug 更深的一层数据问题（影响面可能不止此卡片）。
2. **缓存**：后端 `crud._list_cache` 是进程内内存缓存（有 TTL）、前端 `cachedLoad` 30 秒缓存 → 改完数据要 **刷新页面 + 等 TTL 或重启后端**才看得到新内容。
3. **建议的治本项（未做，避免答辩中途动后端）**：在 `_build_gang_dict` 加"读时兜底"——reason 为空时现场调 `_auto_relation_reason`，这样即使 DB 没填也不至于空白。

**诊断脚本留在项目根**：`_diag_gang_rel.py`、`_diag_gang_rel2.py`、`_diag_api.py`、`_diag_build.py`、`_fix_gang_rel_reason.py`。

---

## 上台前 10 分钟的前端演示体检（15:45）

**服务状态（netstat + curl 实测）**
- **后端 = 5003**（`run-local.bat` 里 `uvicorn main:app --port 5003`）→ **正常**：`/docs` 200、`/api/gangs` 401（需认证，属正常）。
- **前端 = 5173**（`vite.config.js: port 5173`，proxy → `VITE_API_BASE || http://localhost:5003`，配置本身正确）→ **当时没有监听（未在跑）**，已提醒用户自行启动（我这边起不来，见下）。
- 登录口令：`admin / admin123`。根目录 `run-local.bat` 会同时起前后端 → 若后端已在跑，**只用起前端**，否则 5003 端口冲突。

**⚠️ 沙箱限制（重要经验）**：本会话的 Bash 无法启动前端 —— `npm run dev` 触发了 Program Blacklist 里的 **wsl.exe** 被拦；换成 `node node_modules/vite/bin/vite.js` 直启也无输出失败。**结论：涉及 npm/node 启动 dev server 的动作，只能由用户在本机终端执行，不要反复尝试。** 另外沙箱里探测不到监听的端口会返回 **502**（而非连接拒绝），`netstat -ano > 文件` 再 grep 才是可靠判据。

**空表盘点（29 张表，9 张全空）→ 演示要绕开**
- 空：`accounts`、`persons`、`phones`、`evidence_items`、`review_opinions`、`freeze_approvals`、`freeze_receipts`、`merge_suggestions`、`imported_fund_flows`
- 有数据：`operation_logs` 1370、`capital_flows` 949、`cases` 230、`gang_case_relations` 230、`alert_records` 87、`dispatch_orders` 75、`gangs` 49、`key_persons` 25、`graph_edges` **仅 6**、`graph_nodes` **仅 4**
- 建议动线：团伙列表 → 团伙详情 → 案件列表 → 告警中心 → 研判任务 → 审计日志。

**产出**：`答辩PPT/上台前速查卡.md`（服务状态、需绕开的模块、推荐动线、三条被追问口径）。









