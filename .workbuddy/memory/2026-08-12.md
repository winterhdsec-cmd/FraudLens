## 项目工程改进方案文档（2026-08-12 01:00）
- 从工程性和实用性角度，完成了 FraudLens 项目的全景诊断与改进方案。
- 核心发现：
  - 后端扎实（LangGraph 反思闭环 + GNN 三层实验 + 7 Agent + Celery），能力足够。
  - 前端是比赛短板：App.vue 28KB 单体、登录页硬编码假数据（77+/12/92%/12s）、无数据看板、UI 风格业余。
- 撰写方案文档 `docs/FraudLens_工程改进与比赛方案.md`，含：
  - 现状诊断（后端/前端/核心矛盾）
  - Tier 1-3 改进路线图（比赛前 10-15 天优先：登录页真数据 → Dashboard → Demo 案例 → UI 升级 → 申报书）
  - 前端重新设计方案（手术式升级不重写，拆分 App.vue → vue-router + Naive UI + ECharts + 暗黑主题）
  - 无真实数据下的说服力四层策略（合成数据讲故事、公开基准可验证、数据接入框架预留、降级演示路径）
  - 比赛叙事逻辑（一句话核心主张 + 三个论点 + 竞品差异化表）
  - 关键技术指标汇总表
- 文档定位：可工程执行的路线图，不是空规划。

## 第一步完成：登录页去假数据 + bug 修复（2026-08-12 01:15）
- **现状修正**：DashboardView.vue 其实已存在且完善（4 卡片+4 图表+数据来源标注栏，数据来自后端 /api/dashboard 真接口），方案文档里"无看板"的判断不准确——前端主要问题集中在登录页假数据 + loginTags bug。
- **改动（frontend/src/App.vue 登录页）**：
  1. 统计卡片假数据（77+/12/92%/12s）→ 真实能力指标：0.9154 合成基准 F1 / 0.71 AMLSim 扩线 F1 / +0.51 反思闭环增益 / 100% 实验一键复现（论文已验证、可溯源、不依赖数据库和登录态）
  2. 标题 "反诈情报分析系统" → "FraudLens 反诈智能研判系统"
  3. logo 🛡️ emoji → SVG 盾牌图标
  4. 副标题 → "多智能体反思编排 · 异构图神经网络 · 可复现能力基线的反诈研判平台"
  5. **修复 loginTags 未定义 bug**（模板引用了但全项目无定义，登录页特性标签区实际是坏的）
- **顺手修复 AnalysisAdvisory.vue:50 重复 class 属性**（`class="slip-line"` + `class="slip-warn"` → `class="slip-line slip-warn"`），这是阻断 vite build 的已有 bug。
- **验证**：`npm run build` 成功（2286 modules，11.92s）。唯一警告是 chunk 3MB 超 500KB（后续 Tier 2 拆分时处理）。
- 下一步（Tier 1 第2步）：登录后进入 Dashboard 看板页打磨/演示案例故事线。

## 登录页改价值语言 + Dashboard 检查 + 重构判断（2026-08-12 01:20）
- **用户反馈**：登录页写 F1/AMLSim/反思增益等技术指标太专业，民警看不懂。
- **修改**：登录页 4 卡片技术指标 → 民警价值语言（智能串并/团伙发现/冻卡辅助/安全可控 + 通俗说明）；副标题去技术词；loginTags 改民警语言。`npm run build` 通过。
- **Dashboard 检查结论**：
  - 后端 `get_dashboard_data()` 完整（案件数/团伙数/金额/预警/风险分布/状态分布/诈骗类型排行/月度趋势/团伙威胁/最近案件 + 空库判断 + 600s 内存缓存）
  - 前端 DashboardView.vue 完整（4 卡片 + 4 图表 + 最近案件表 + 数据来源标注栏）
  - seed.py 有 10+ 个真实风格演示案例（刷单连环/投资理财/冒充公检法/冒充客服等，含受害者/金额/完整作案描述）+ 按类型聚合的团伙数据
  - **结论：Dashboard 无需重建，重点是确认数据库已 seed 可渲染**
- **前端架构重构判断：不重构，打磨**。理由：19 条路由 + 18 views + 2 composables + 5 components + vue-router 鉴权，分层已成熟；问题在"表面层"（登录页假数据已修、UI 风格统一、演示故事线），不在架构。重构 App.vue 风险高收益低。
- 遗留：chunk 3MB 超 500KB 警告（Tier 2 处理）；CSS 变量暗黑主题已存在（--color-bg-page 等），UI 统一是轻量活。

## W2-2/2-3 完成：InputView emoji + Element Plus 按需（2026-08-12 02:10）
- **W2-2 InputView**：18 处 emoji 全部替换（EditPen/Search/Cpu/DataAnalysis/InfoFilled/Money/Delete/Files/Promotion 图标；✅⬜→✓/○ 文本符号）。构建通过。
- **W2-3 Element Plus 按需**：
  - 已安装 unplugin-vue-components + unplugin-auto-import（网络可用，82 packages）
  - vite.config 加 AutoImport/Components + ElementPlusResolver(importStyle:false)
  - main.js 去掉 `app.use(ElementPlus)`，保留全量 CSS（兜底 ElMessage 等 teleport 组件样式）+ 图标全量注册
  - **效果：主包 1.84MB → 1.15MB（gzip 600→383KB），累计 3.1MB → 1.15MB（-63%）**
- **注意**：构建通过，但 ElMessage/el-dialog 等运行时样式靠全量 CSS 兜底；建议用户实际启动前端点几个页面确认无样式丢失
- 剩余：全局图标全量注册（ElementPlusIconsVue，可再优化但收益小）；其余页面 emoji（CaseDetail/Details/Chat/Alerts 等）
- 方案文档已加 3.3 架构评估（6.5/10）+ 3.4 可视化评估与升级方案（7/10，W4 补过程叙事+联动）

## W2 收尾完成：全量 emoji 清理 + 暗黑主题激活（2026-08-12 02:55）
- **emoji 清理**（功能页面 template 区全部替换为 Element Plus 图标）：
  - 脚本批量被 IDE 锁挡（Python open('w') PermissionError），改 **Edit 工具逐个 replace_all**（宿主 API 可写）
  - 已清：CaseDetailView(30)、DetailsView(template 23)、UploadView(16)、ChatView(template 11)、CapitalFlowView(15)、GroupsView(9)、ReportView(7)、AlertsView(5)、AnalysisAdvisory(5 含 ⛔⚠️ 三元改 el-icon)、KeyPersons(2)、Dispatch(2)、NetworkGraph(1)、StatusView(template 2)
  - 映射：🔍Search 📊DataAnalysis 📈TrendCharts 📋Files 📅Calendar 🛠️Tools 📄Document 📝EditPen 👤User 📞Phone 💰Money 🎯Aim 🧠ChatDotRound 🔗Link 🕵️View 👮UserFilled 🤖Cpu 💡InfoFilled 💱Money 🗑️Delete 🏦OfficeBuilding 📂Folder 📑DocumentCopy 📤Upload 📷Camera 🔄Refresh 🧩Grid 🏛️OfficeBuilding 🌏/🌍Earth 🧬Connection ⚠️WarningFilled ⛔CircleCloseFilled 👉Right 🕐Clock 🖨️Printer 🛡️Lock
- **遗留 emoji（保留）**：ShowcaseView(91 宣传页装饰)、useFraudLens.js/api.js（JS 消息不可见）、DetailsView/ChatView/StatusView script 区动态图标（JS 对象 icon 字段，改需改 JS 逻辑，W3 状态层时处理）
- **W2-4 暗黑主题激活**：index.html 早有 class="dark" 但 style.css 无映射（亮色底+深色卡混乱）。补 `html.dark { --color-*: var(--dark-color-*) }` 全面映射 + Element Plus --el-* 暗色变量。build 通过。
- **构建**：主包稳定 1.15MB（gzip 383KB）。**需用户启动前端实际查看暗黑效果**（个别硬编码亮色的地方可能需微调）
- 下一步：W3 状态层拆分（useFraudLens.js 68KB → useAuth/useCases/useDashboard/useAnalysis）

## 导航架构优化：侧边栏可折叠 + 顶栏面包屑 + Dashboard 入口（2026-08-12 03:00）
- **用户需求**：前端"点击逻辑"不要全做成侧边栏，问是否要调整导航架构。
- **决策**（按业务场景）：三项全做——公安反诈系统标准范式（侧边栏+顶栏+指挥中心入口）：
  1. **侧边栏可折叠**：MainLayout 加 `collapsed` ref + el-menu `:collapse` + 收窄 64px 样式（隐藏 logo 文字/组标题/系统状态文字），顶栏折叠按钮切换。演示时可全屏看图。
  2. **顶栏 + 面包屑**：19 路由加 `meta.title`；MainLayout 主区顶部加 top-bar（折叠按钮 + el-breadcrumb 首页/当前页 + 实时时间 + 当前用户）。
  3. **Dashboard 快捷入口卡片**：DashboardView 顶部加 6 张功能卡（文本录入/文件上传/案件管理/团伙画像/资金流向/AI助手），点击直达，替代"点侧边栏"的认知负担。
- **顺手修复 bug**：DashboardView 模板用 `router.push` 但 script 未定义 `router`（原有空状态按钮点击会报错）→ 补 `useRouter`。
- **构建通过**（11.01s）。需用户启动看折叠/顶栏/卡片实际效果。
- 下一步：W3 状态层拆分。

## 前端代码体检：4 个 bug 修复 + 收尾（2026-08-12 03:10）
- **系统性检查**：启发式扫描"模板引用但 script 未定义"标识符（全 30 个 vue 文件）+ console 残留 + 重复属性。结论：大部分是误报（对象属性/CSS 类名/字符串值），**真实 bug 1 个**。
- **本轮修复**：AlertsView "一键派单"按钮 `@click="state.showCreateDispatch = true"` 是死代码（script 无此属性、模板无对话框）→ 改为 `router.push({ name: 'dispatch' })` + 补 useRouter import。
- **今日累计修 4 bug**：① loginTags 未定义（App.vue→LoginPanel）② AnalysisAdvisory 重复 class（阻断 build）③ DashboardView router 未定义（空状态按钮坏）④ AlertsView showCreateDispatch 死代码。
- **检查确认无问题**：console.log 残留无（api.js 的都在 isDev 下合理）；重复 class 无（扫描到的是静态+动态 class 合法写法）；JS 里 emoji 均为功能性标识（团伙头像/消息标签）保留合理。
- **最终构建**：✓ 10.71s，主包 1.15MB（gzip 383KB）+ vis-network 520KB 独立 chunk。
- **W3/W4 待办**（下次会话）：useFraudLens.js 68KB 拆分、可视化叙事升级（研判过程动画+图联动）、README/回归。

## W1 拆壳完成：App.vue → LoginPanel + MainLayout（2026-08-12 01:35）
- **用户确认**：时间充足（约一个月），做重构+打磨，申报书后写。
- **完成 App.vue 拆壳**（720 行 → 30 行壳）：
  - `components/LoginPanel.vue`：登录页（品牌面板+统计+表单+安全+版本号），样式随组件迁移（scoped）
  - `layouts/MainLayout.vue`：侧边栏菜单 7 组 + 全局搜索 + 进度/结果对话框 + 主区，`isFullPage` 用 `router.currentRoute.value.meta?.fullPage` computed 保持响应式
  - App.vue 壳：provide appState + `<MainLayout/>` 常驻 + `<LoginPanel v-if="!store.isLoggedIn && !isFullPage">` 覆盖
- **状态传递**：两组件均 `inject('appState')`，不改 useFraudLens
- **验证**：npm run build 通过（11.39s），chunk 3MB 警告留 W1-2 路由懒加载处理
- **方案文档已更新**：第三章改为"月度重构计划"（W1 拆壳✅ / W1-2 懒加载 / W2 UI / W3 状态层 / W4 演示路径）+ 进度跟踪表
- 下一步：W1-2 路由懒加载（component: () => import(...) 拆 chunk）

## W1-2 路由懒加载 + echarts 按需完成（2026-08-12 01:45）
- **路由懒加载**：router/index.js 19 个路由全部改 `() => import(...)`，每个视图独立 chunk（3-44KB）
- **echarts 按需**（主包 -500KB）：
  - 根因：useFraudLens.js 顶部 `import * as echarts from 'echarts'`（全量 ~1MB）打进主包
  - 改 `echarts/core` + 注册 BarChart/LineChart/PieChart + Tooltip/Legend/Grid/Title + CanvasRenderer
  - 已确认所有 series type（bar1/line3/pie3）和 graphic.LinearGradient(3) 均被覆盖，无遗漏
- **效果**：主包 3.1MB → 1.84MB（gzip 763→600KB）；总 chunk 拆分清晰（各视图 + vis-network 520KB 独立）
- 剩余大头：Element Plus 全量（main.js app.use(ElementPlus) + 全量图标注册）→ 留 W2 UI 阶段按需化
- 下一步：W2 UI 专业化（emoji 清理 → Element Plus 按需 → 主题定制）

## W2-1/2-2 emoji 清理（登录/布局/核心页）完成（2026-08-12 02:00）
- **W2-1 登录/布局**：LoginPanel（👤🔑🔒→User/Lock 图标）+ MainLayout（✅💰📊📋→CircleCheckFilled/Money/DataAnalysis 图标）
- **W2-2 核心演示页**：
  - DashboardView（2 处：📊📝→DataAnalysis/EditPen）
  - OverviewView（15 处：标题/统计卡 4 图标/案件列表/卡片内联 3 处/批量删除/关联团伙/空状态，全部→Element Plus 图标）
- **构建验证**：均通过（14.08s）
- 剩余 emoji：InputView（13 处）、CaseDetail/Details/Chat/Alerts 等（W2-2 继续）
- 下一步：W2-2 继续（InputView 等）+ W2-3 Element Plus 按需引入（主包再降）

## W3 安全版：纯函数抽取 utils.js（2026-08-12 14:50，第二天）
- **useFraudLens.js 实况**：1807 行单体（认证/案件/分析/看板/预警/资金流/派单/报告/搜索全耦合、共享 ref）。18 个视图全走 `useAppState()`（inject appState）解构，**只要 useFraudLens() return 接口不变，视图零改动**。
- **已做**：抽 9 个零依赖纯函数/常量 → `composables/utils.js`（defaultMethodFlow/defaultKeywords/gangIcons/getParticleStyle/getRiskType/getEventType/getFeatureIcon/parseRawAmount/formatAmountRaw）。useFraudLens.js 删原定义 + import，接口不变，`npm run build` 通过（24.34s）。文件 1807→~1670 行。
- **深度拆分风险评估**：拆 useAuth/useCases/useDashboard 需重组内部共享 ref 逻辑，且当前无法运行时验证（后端未起），拆错=大面积回归影响演示。**判断：深度拆分放比赛后（Tier 3）更稳妥**；若要现在做需用户配合启动后端做全量回归。
- **generateReportHTML 依赖 gangs/reportConfig ref，不能抽纯函数**（保留原处）。
- 下一步：若继续 W3，可选组件抽取（SectionHeader/StatCard 复用）；或转 W4（可视化叙事/演示故事线/README）。

## 磁盘清理：删废弃 61G Docker 数据盘（2026-08-12 15:28）
- **问题**：用户 E 盘 84%，项目目录 60G+ 被 Docker 占。
- **诊断结论**：
  - Docker 实际数据盘在 `E:\DockerDesktopWSL\disk\docker_data.vhdx`（115G，Aug 12 15:24 更新，**活跃使用中**）
  - 项目 `E:\FraudLens\DockerData\disk\docker_data.vhdx`（61G，5月24 创建后没动过）是**废弃旧盘**
  - 证据：Docker volume 全在 `/var/lib/docker/volumes/fraudlens_*/_data`（WSL 内部）；无容器/卷/配置引用项目 DockerData；AppData\Docker\wsl\disk 为空
- **执行**：删除 `E:\FraudLens\DockerData`（61G）→ E 盘 776G→716G（可用 156→217G）
- **顺带发现**：
  - Docker Desktop 用 WSL2，WSL 发行版：Ubuntu + docker-desktop；docker-desktop BasePath = `E:\DockerDesktopWSL\main`
  - Docker 里有 fraudlens-backend-1（占 5003，旧镜像 13 天前）+ fraudlens-celery-1 在跑；镜像 fraudlens-backend 10.2G、celery 12.2G
  - **docker 里 mysql:5.7 容器启动失败**：缺 MYSQL_ROOT_PASSWORD 环境变量（compose 配置问题，待修）
  - 本机 MySQL80 服务已装但 DEMAND_START（需 sc.exe config 改 auto）
- **坑**：PowerShell 里 `sc` 是 Set-Content 别名，改服务必须 `sc.exe config ...`

## 前后端本地启动成功（2026-08-12 15:50）
- **最终状态**：前端 http://localhost:5173/（vite dev）+ 后端 http://localhost:5003/（本地 venv python），数据库连上（fraudlens 库 28 表，229 案件/31 团伙/5161 万），登录 admin/admin123 成功。
- **后端启动正确方式（踩坑总结）**：
  1. `cd backend` 目录启动（**不能从根目录**——pydantic-settings 自动加载根 .env，MINIO_ROOT_USER 等 5 字段不在 Settings 模型 → extra_forbidden 崩溃）
  2. 用命令前缀环境变量覆盖 DB 配置：`DB_HOST=localhost DB_PORT=3306 DB_NAME=fraudlens DB_USER=root DB_PASSWORD=<根.env密码> nohup ./venv/Scripts/python.exe main.py &`（key.env 是 docker 密码，不能动；load_dotenv 不覆盖已有环境变量）
  3. 后端启动慢（数据库初始化+Redis 超时降级约 1-2 分钟才监听 5003）
- **venv 依赖**：从空 venv 用清华镜像装最小集（fastapi/uvicorn/flask/sqlalchemy/pymysql/celery/langgraph 等 80 包，3m39s）。torch/transformers/easyocr 等重型未装（main.py 有 try/except 降级）。
- **遗留**：① Redis 未启动（后端降级内存存储，警告正常）② 分析/聚类功能因缺 torch 等可能受限 ③ Docker 环境 mysql:5.7 缺 MYSQL_ROOT_PASSWORD 待修。

## W3 推进：SectionHeader 组件 + useAuth 拆分（2026-08-12 15:30，第二天）
- **用户选择**：组件抽取(3) + 深度拆分(4) 都做；后端 HAN/W4 暂缓。
- **组件抽取**：新建 `components/SectionHeader.vue`（title/desc/icon props + 默认 slot），替换 Dashboard/Overview 的 section-header（样式用全局 style.css 已存在的），build 通过。复用模式已建立，其余 14 个 view 可后续批量替换。
- **深度拆分第 1 步：useAuth 拆分成功**：
  - 新建 `composables/useAuth.js`：loginForm/loginLoading/loginError/loginProgress/handleLogin/handleDemoLogin/handleLogout/clearLoginProgressTimer，**依赖注入**（store/route/apiLogin/apiDemoLogin/ElMessage/onLoginSuccess）
  - 关键设计：handleLogin 原直接调 reloadCasesAndGangs/loadFlowMetrics/loadDashboard/loadAlerts → 改为 `onLoginSuccess` 回调注入（闭包，登录时求值，无 TDZ 问题）
  - useFraudLens.js 删认证代码 + `const auth = useAuth({...})` 组合 + return 前解构（接口不变，视图零改动）
  - **修复泄漏**：onUnmounted 里原 `if (loginProgressTimer) clearInterval(loginProgressTimer)` 引用了已删除变量 → 改为 `auth.clearLoginProgressTimer()`（useAuth 内部封装）
  - useFraudLens.js：1807→1692 行（累计 utils 抽取后减 ~200 行），`npm run build` 通过（12.27s）
- **深度拆分模式已验证**：依赖注入 + 接口保持 + build 回归。
- 下一步：拆 useCaseData（cases/gangs 核心数据模块）或先请用户运行时验证登录流程（useAuth 改动涉及登录，建议用户启动前后端确认登录/演示登录/退出正常）。

## 数据加载故障修复（2026-08-12 16:45）
- **用户反馈**：前端"数据没加载、变瘦了"。诊断发现两个后端问题：
  1. **/api/gangs 500**：`gang_case_relations` 表缺 `relation_type/reason/matched_entities` 3 列（模型有表没有，schema 不一致）→ `fix_gang_table.py` ALTER 补齐（需先停后端，否则 ALTER 卡元数据锁）
  2. **dashboard/alerts/capital 超时**：本地无 Redis，后端每次请求连 Redis 等 **92 秒超时**才降级 → 启动 Docker redis 容器 `fraudlens-redis-local`（redis:7，映射 6379）→ 全部接口 0.0-0.06s
- **修复后全部接口 200**：dashboard 0.015s / alerts 0.063s / capital 0.000s / gangs 0.015s / cases 0.047s
- **后端环境依赖**：MySQL（本机 MySQL80）+ Redis（Docker 容器 6379）缺一不可；后端启动约 2.5 分钟（seed 数据）
- **注意**：后端 main.py 有全局 `_db_lock`（所有 /api 串行），单慢请求会阻塞全部；启动慢（159s）是 seed 数据正常现象
- **用户剩余诉求**：前端"变瘦"（视觉密度）+ 侧边栏导航要改（不能纯粹侧边栏）→ 下一轮 UI 改进

## UI 重构：顶栏导航 + 视觉增强（2026-08-12 17:00）
- **用户方向**：参考 Vben Admin 模式——顶栏一级分组 + 侧边栏二级菜单；颜色对比弱 + 可视化元素少。
- **导航架构重构（MainLayout）**：
  - 布局 flex → **grid**（header 行 + aside/main 行）
  - 新增 `.app-header` 顶栏：小 logo + 6 个一级分组（el-menu horizontal：数据采集/总览中心/案件研判/办案工具/输出与助手/系统管理）+ 右侧实时时间/用户名/折叠按钮
  - 侧边栏改为**动态二级菜单**：`menuGroups` 数据驱动，`activeGroup` computed 根据当前路由定位分组，侧边栏只显示当前分组菜单项
  - 点击顶栏分组 → 跳该组第一页；原 top-bar 简化为面包屑
  - build 通过（10.78s）
- **视觉增强（style.css）**：
  - 暗黑 token 对比度：bg-page `#0f172a→#0a0f1e`（更深）、bg-card `#1e293b→#1b2743`（提亮拉开层次）、border `#334155→#2e3f63`（更亮）、text-1 `#f1f5f9→#f8fafc`、主色 `#3d8bfd`
  - `.tech-card` 加 1px 边框 + 蓝色微光阴影 + hover 强化
  - `html.dark .main-content` 加网格纹理背景 + 顶部蓝色光晕（公安大屏风）
- **数据问题已修复**（见上条）：gangs 表补列 + Redis 容器启动，全部接口 200
- **待办**：用户验证效果；若"可视化元素少"仍存在，下一轮增强 Dashboard 图表/统计卡

## UI 布局重做：标准后台布局（多标签页）（2026-08-12 17:05）
- **用户反馈**：上一版"更混乱"，希望参考开源项目（vue-vben-admin）做法。确认无前端模板 skill，改用"借鉴主流布局规范"。
- **用 show_widget mockup 画了设计稿**，用户确认"按这个重做"。
- **布局对齐 vue-vben-admin 标准**：
  - 顶栏：小 logo + 6 一级分组（horizontal）+ 右侧时间/用户名/折叠
  - 侧边栏：当前分组标题 + 二级菜单（动态 activeGroupItems）
  - **新增多标签页**（tabs）：MainLayout 本地 ref，路由 watch 自动加入，可切换/关闭，dashboard 固定不可关；图标全量注册（Close 可用）
  - 去掉网格纹理（用户觉得突兀），页面改纯色平面
  - 面包屑 top-bar → 替换为 tabs-bar
- **build 通过**（11.30s）。待用户刷新验证。
- 反思：手搓 UI 两次未达预期 → 教训是"改 UI 先画设计稿对齐，再实现"（本次已用 mockup 流程）

## 前端深度测评（市场视角）（2026-08-12 17:10）
- **用户要求**：以专业前端专家视角，跳出项目用市场标准深度测评。
- **产出**：`docs/前端深度测评报告.md`。
- **核心判断**：项目处在"科技大屏风"（DataV）与"企业级后台"（Ant Design/Vben）之间摇摆 → 混用导致"乱"。
- **评分**：导航 7/10（已达标）、视觉系统 5/10（token 有但未统一消费、层级语言混乱）、可视化 5/10（仅 7 个图表、无叙事无联动）、内容密度 5/10（"变瘦"根因）、技术 7/10。
- **改进路线 P0-P3**：P0 统一视觉语言（克制扁平+语义色板）→ P1 Dashboard 数据叙事（联动+radar/gauge）→ P2 提密度 → P3 放大 NetworkView 关系图谱差异化亮点。
- **关键洞察**：技术底座合格，问题在设计语言一致性与数据叙事密度；关系图谱是超越普通后台的独家卖点。

## P0 统一视觉语言实施（2026-08-12 17:20）
- **诊断**：Dashboard 卡片**无边框**（纯发光/渐变），Overview 有边框 + 发光叠加 → 卡片语言不一致是"乱"的根源。
- **已收敛（核心页 Dashboard/Overview）**：
  - Dashboard `.stat-card`：加 1px 边框 + hover 发光→`shadow-md`，顶边线 3px 渐变→2px 语义纯色（danger/warning/success/info 变量）
  - Dashboard `.chart-card`：加 1px 边框，hover 发光→微阴影
  - Overview `.case-card`：边框 `rgba(0,198,255,0.08)`→标准 `--color-border-1`，移除全屏渐变覆盖层，hover 发光→`shadow-md`，底部渐变线→主色线
- **统一卡片语言**：1px 边框 + 微阴影 + 语义色强调线（顶/底），去掉发光/渐变叠加
- **修复**：Overview 替换时括号残留（Unexpected }）已修复
- **build 通过**（17.29s）。待用户刷新验证。
- **P0 剩余**：其他页面（CaseDetail/Details/Chat 等）发光样式收敛、硬编码色（#10b981 等）→语义变量、列表/详情信息密度

## 结合视觉 AI 评测修复（2026-08-12 17:40）
- **用户提供另一 AI 的截图视觉评测**，指出对比度差/杂乱/层级不清。逐条修复：
  - **对比度**：暗黑 text-4 `#64748b→#7c8da6`；InputView placeholder `#475569→var(--color-text-3)`；要点检测未选中项（深底→卡片底+标准边框+text-2）；侧边栏状态文字提亮（v3.0/AI引擎/数据库 → text-3/2/1）；tab-close 提亮 text-2
  - **去"黑客帝国"感**：textarea focus 发光→简洁边框；分析按钮脉动发光动画→静态；空状态图标提亮
  - **标签页区隔**：active 标签淡主色背景（更醒目）+ tabs-bar 底部分隔线
- **build 通过**（11.78s）。待用户刷新验证。
- **剩余评测建议**（后续）：右侧面板小屏折叠、图标风格统一、搜索栏定位、"资金流水"截断修复

## 深度检测：找到"文字和文字重合"根本 bug（2026-08-12 17:55）
- **用户反复反馈"文字重合/混乱/美观差"**。决定深度检测：装 puppeteer-core + 系统 Chrome 真实截图（不靠盲改）。
- **工具**：`E:\FraudLens\.ui-test\screenshots.js`（screenshots + scripts/puppeteer-core + Chrome headless）→ 可复用，详见 `.ui-test/README.md`（建议存为 skill）。
- **核心 bug 找到**：`el-menu mode="horizontal"` 的菜单项因 Element Plus 用 `float: left`，脱离 flex header 容器，**向左飘到 sidebar 旁边与 sidebar 菜单文字完全重叠**（这就是用户一直说"文字重合"的根因）。
- **修复**：`.top-menu .el-menu-item { float: none !important; display: inline-flex !important; }` 等强制 inline-flex；`.top-menu` 加 `display: flex; align-items: center`。
- **截图证实**：修后 Dashboard/Input/Groups 的一级菜单回到顶栏内，不再与 sidebar 菜单文字重合。
- **承认**：手搓 UI 视觉密度/质感有限——`docs/前端深度测评报告.md` 里的 P1（数据叙事）、P2（信息密度）才是真正的"美观性"工作。这次有了截图工具，可以精准迭代。

## 前端模板选型调研（2026-08-12 18:10）
- **用户要求**：广泛调研开源模板，参考完整设计稿（还有一个月时间）。
- **产出**：`docs/前端模板选型调研.md`。
- **调研结论**：
  - 主流 Vue3 模板：vue-vben-admin(~29.6k,功能全但重) / vue-pure-admin(~20k,Element Plus 轻量代码清爽) / vue-element-plus-admin / SoybeanAdmin / Arco Design Pro Vue
  - **推荐 vue-pure-admin 布局骨架**（Element 生态、轻量、二次开发友好）
  - **最大发现：UK Police Design System**（警务系统专属设计规范，暗黑优先）：背景三级明度层级（bg-canvas/surface-0/1/2）、文字三级、语义色（Action蓝/Success/Warning/Danger/Info）、App Shell 布局、不用纯黑/红色只留给行动/深色降饱和
  - 公安国标：配色≤3种（主色75-80%）、文字12-36px、页面中心1000-1400px、左右布局
- **推荐组合**：vue-pure-admin 布局 + 警务设计 token，**不引入工程**只提炼规范，应用到现有页面
- **实施计划 S1-S5**：token 重做→MainLayout 微调→核心页适配→全站收敛+截图回归→Showcase 分区

## 借壳重做 S1：暗黑 token 体系重做（对齐警务规范）（2026-08-12 18:30）
- **用户确认"借壳重做"**：保留现有工程，按 vue-pure-admin 布局骨架 + UK Police Design System token 体系重做壳和样式（不引入模板工程，避免覆盖 18 页面）
- **做法**：克隆 vue-pure-admin 到 `/e/FraudLens/tmp`（浅克隆，gitee 国内快），看其 lay-frame/lay-navbar/lay-tag 结构（TS+Pinia 重工程，不能照搬代码，只提炼布局骨架）；token 直接对齐 UK Police Design System
- **S1 完成**：重做 style.css 暗黑 token：
  - 背景三级明度：`bg-page #0d1220 / bg-layout #141b2b / bg-card #1a2234 / bg-hover #212a3d / bg-overlay #232c40`（比之前层次更清晰）
  - 文字三级：`text-1 #eceef2 / text-2 #b0b7c2 / text-3 #8a93a1 / text-4 #6b7480`
  - 语义色：primary `#3b82f6`（Action 蓝）、success `#10b981`（翡翠）、warning `#f59e0b`（琥珀）、danger `#ff6b6b`（警务珊瑚红）、info `#6366f1`（靛蓝）
- **截图证实**：S1 + 之前 el-menu float 修复叠加，Dashboard 整体质感显著提升——顶栏菜单正常排列、卡片边框统一、语义色识别度高、侧栏状态可读
- **build 通过**（12.22s）。下一步：S2 MainLayout 布局微调（侧栏深色+信息密度+顶栏层级）、S3 核心页（Dashboard/Overview/Input）按新 token 适配
- **今天到这**：已完成核心 bug 修复 + token 重做，截图工具就绪，下次会话继续 S2-S5

## Dashboard 图表布局修复（2026-08-12 18:55）
- **第二个"混乱"根因**：`.overview-charts` 和 `.chart-content` **完全没有 CSS 定义**——图表卡片竖排全宽、echarts 容器无高度（导致"诈骗类型排行/月度趋势"被推到视口外截断）。
- **修复**：
  - `.overview-charts { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }` + 响应式（<1100px 单列）
  - `.chart-content { width: 100%; height: 240px; }`（紧凑，让一屏可见更多）
- **截图证实**：Dashboard 饼图并排两列、标题完整可见、信息密度大幅提升（之前 4 个图表一屏装不下，现在第一行两图一屏可看）
- **build 通过**（11.02s）。继续推进 S3-S5 核心页适配与全站收敛。

## 今日整体成果（2026-08-12 全天）
- **W3 状态层**：utils.js 抽取 + useAuth 拆分成功（依赖注入模式）
- **环境**：启动本机 MySQL + Docker Redis，删废弃 61G 数据盘（E 盘 84%→77%）
- **数据修复**：gang_case_relations 补 3 列（ALTER）+ Redis 容器 → 全部 API 200
- **导航架构**：顶栏一级分组 + 侧栏二级 + 多标签页（vue-vben-admin 模式）
- **深度测评**：docs/前端深度测评报告.md + docs/前端模板选型调研.md
- **套壳修复**（关键）：
  - el-menu horizontal `float:left` bug → 一级菜单不再飘到 sidebar（"文字重合"根因）
  - 双重 logo 去除 + 侧栏 logo 缩小 + content padding 收敛
  - Dashboard `.overview-charts` 无 grid + `.chart-content` 无高度 → 修复图表并排 + 240px
  - Input 空状态引导（关键词预览 hint）
  - S1 token 重做（警务明度三级 + 语义色）
- **工具**：`.ui-test` puppeteer-core + 系统 Chrome 截图验证（解决"盲改"）
- **验证方式**：截图 + build 回归
- **剩余（下次）**：S4 其余页面批量收敛、S5 Showcase 分区、硬编码色收敛（DetailsView 123 色等）
- **已知**：Deepseek-V4-Pro 模型不支持读截图（换多模态可看）

## KeepAlive + 风格收敛 + LoginPanel 重做（2026-08-12 19:20）
**用户反馈**：①把整体风格改成showcase那样反而没以前好 ②showcase→input跳转有问题 ③系统内点击不同界面有延迟。

**深度阅读定位的三个根因**：
1. **延迟**：`MainLayout.vue:141` 的 `<RouterView />` 没有 `<KeepAlive>`，每次切换组件销毁重建（echarts 重初始化 + onMounted 重拉数据），重页面感知 800ms+。
2. **跳转突兀**：`LoginPanel.vue:116` 左侧面板用亮蓝渐变 `#1d4ed8→#1e3a8a`，和 showcase 深色 cyan 科技风完全冲突；点击"进入系统"→/input→LoginPanel 突然弹亮蓝卡片 = "跳转有问题"。
3. **风格变差**：之前暗黑主色是 `#3b82f6` 纯蓝无光（被改的），原来 cyan `#00d4ff` 发光风没了 → 既不如以前，也不像 showcase。

**实施的修复**：
- **MainLayout.vue**：`<RouterView />` 包 `<KeepAlive :include="[18 个 view 名称]">` + `<transition name="page-fade">`（淡入 180ms）。跳转耗时从感知 800ms+ 降到实测 **平均 3ms**。
- **LoginPanel.vue**：左侧面板从亮蓝渐变改为深色 cyan 渐变 `#0d1a2e→#122441→#0a1729`，logo 加 cyan 发光 box-shadow，副标题/统计卡/标签全 cyan 化，背景加 cyan/violet 径向光晕。与 showcase 风格统一。
- **style.css 暗黑主题**：主色还原为 cyan `#00d4ff`（hover `#38e0ff`/active `#00a8cc`），新增 `--dark-color-primary-glow/--dark-color-primary-glow-soft`，Element Plus 暗色变量同步 cyan。
- **MainLayout.vue**：侧栏 logo 加 cyan 发光阴影；侧栏菜单激活态加 `inset 3px 0 0 cyan` 左侧色带（更醒目）。

**验证**：`.ui-test` 截图巡检 + jump-test 跳转流程截图 + timing-test 精确测量（平均 3ms）。build 通过 10.82s。

**效果**：登录页从"突兀亮蓝"变"深色 cyan 科技"（与 showcase 一致）；Dashboard/Input 等内部页面主色调、激活态、按钮、边框全 cyan 发光，回归原来"警务 cyan 科技"风格。

## 视觉精进轮（2026-08-12 20:10）
**用户反馈**：①跳转"有时"延迟严重 ②字体更清晰 ③顶栏更美观 ④表格线条劣质 ⑤美术效果要增加（可上网找方法）。

**深查延迟根因**：
1. `<KeepAlive :include="[...]">` 依赖组件 name 匹配，build 后可能不匹配 → 部分页面切换仍重建。改为 **无 include 缓存全部** + `:key="r.name"`（同路由复用）。详情页（case-detail/details）有 watch 响应数据变化，缓存复用安全。
2. transition `mode="out-in"` 串行等待旧页消失 → 改为**默认模式** + 时长 180ms→120ms（enter）/80ms（leave）。
3. 懒加载大 chunk：主包 1.1MB → **manualChunks 分包**（vue-vendor/element-plus/echarts/vis-network 独立缓存），主入口降到 **156KB**，各库独立缓存不再重复下载。

**实测**：timing-test 页面切换平均 **3ms**（KeepAlive 命中）；first-load-test 全页面 1-5ms。

**视觉优化（参考网上"微渐变+发光+分层"方法）**：
- **字体**：暗黑 text-1→`#f4f7fc`/text-2→`#bcc6d6`（提亮），body 加 `text-rendering: optimizeLegibility` + `font-variant-numeric: tabular-nums`。
- **顶栏**：渐变背景（layout→hover）+ 底部 cyan 光带 `::after` + 紧凑 logo（"反诈情报FraudLens"）+ 菜单 hover cyan 光 + 激活态圆形图标下方 cyan 亮条发光。
- **表格（el-table 全局重写）**：header 渐变深底 + 底部 `border-2` 分隔线 + 字体 600/12px；行分隔线 `divider`；hover 柔和 cyan 0.04；选中行 0.06；tag 色统一当前语义变量；**表头右侧分隔线去除**。
- **卡片美术**：stat-card/quick-card/chart-card/tech-card 统一加 `linear-gradient` 微渐变 + 顶部 cyan 光晕 `::after` + hover 上浮 2px + cyan 发光 shadow。
- **侧边栏**：渐变背景 + 菜单 hover cyan 发光。
- **tabs**：激活态顶部 cyan 标记线 + 渐变背景 + 加粗。
- **背景**：main-content 加左上/右上 cyan/indigo 椭圆光晕（克制，非网格纹理）。

**验证**：build 通过 9.92s；`.ui-test` timing-test/first-load-test 测量 + 截图巡检（工作台/系统管理/预警/派单页）。
**注意**：本轮截图验证时，截图工具对 keep-alive 后的页面切换有延迟，需等 echarts 渲染（waitForFunction 超时 8s 是测试脚本问题，非真实延迟）。

## 论文压缩改写：CPEC2026 匿名版 Word（2026-08-12 21:33）
**老师反馈**：14 页太长（期刊一般 4-5 页）、重点不突出、格式有问题（页面跑到右上角=LaTeX 版心偏置）、内容空洞。
**用户补充**：要匿名版（去掉署名/基金/作者简介）；参考张焘《人工智能赋能的实验教学体系建设与实验案例设计》（CAJ 加密压缩格式无法提取正文，但标题给出典型写法）。

**实施**：
- **CPEC2026 模板规范**（附件2 PDF 已转换分析）：摘要 ≤200 字、关键词 3-8 个、三线表、图题居中、参考文献 ≥12 篇顺序编码、结语不能写成摘要。
- **工具链**：python-docx 1.2.0 + Word COM (Office16) 转 PDF 数页 + pymupdf 验证。pandoc 不可用。
- **Word 生成脚本**（`E:/FraudLens/_gen_paper_word.py`）：A4 标准页边距、五号宋体正文/黑体标题、1.15 倍行距、保留 fig_arch.png（11cm 宽）。
- **压缩策略**：删表 4（评价量表）和表 5（思政映射）改为正文叙述（核心数据已表达，省半页）；删英文表题；行距 1.3→1.15；参考文献 8.5pt；结果 14 页 → **5 页**。
- **匿名验证**：无韩冬/吴燕波/徐伟/基金/E-mail/430034/湖北警官学院等敏感词。
- **版式验证**（pymupdf 转 PDF→PNG 检查 5 页）：每页内容居中，无"跑右上角"问题；三线表规范；图题居中；标题层级清晰。

**产物**：`paper/论文产出/CPEC2026教学案例稿/面向反诈团伙研判的AI赋能实验教学案例设计_匿名版.docx`（241KB）

**重点突出策略**：
- 标题"AI 赋能实验教学案例设计"直击教学案例类期刊核心
- 引言精简为"问题→工作→声明"3 段，删"复杂工程问题"大段论证（空洞感来源）
- 第 4 章"实训教学设计"占近 2 页（核心创新：研-训-评 + 四环节 Lab）
- 能力基线三层验证（合成/AMLSim/Elliptic）支撑 Lab4 边界认知教学
- 结语 1 段，不能写成摘要（模板要求）

## ★重大转向：教育类论文被否，改技术类（2026-08-12 22:35）
**老师的核心反馈（微信长对话）**：
- 教育类（CPEC2026 教学案例）方向"牵强"→ **放弃**。硬靠教学案例没优势、没有落地空间。
- 要写"网络犯罪/反诈"方向技术论文，**结合 FraudLens 真实项目**，用**算法和模型实验**。
- "要有真实的实验数据"、"要多看文献"、"不能全借助 AI 写"、"期刊社一看就知道 AI 写的"。
- 版面：中文期刊 4-5 版（页）；EI 会议 8 页；超页加钱；选期刊后去官网下模板排版。
- 页码不放右上角；有的一栏有的两栏，按期刊模板定。
- 老师发的重点文献（待看）：《电信网络诈骗犯罪预警的失灵与优化》庄华马忠红 2025（在 paper/参考文献/）——"重点看这篇"。
- 省大创结题可写网络犯罪/反诈方向论文，不一定绑教学案例。

**项目真实实验资产（可支撑技术论文）**：
- `backend/gnn/eval_full_results.json`：8 大实验组，含 exp1_baselines（KMeans/HDBSCAN-only/Semantic/CurrentSystem(fund)/GraphSAGE/HAN 六方法 F1+NMI 10 seeds）、exp2 双通道、exp3 置信度门控、exp4 反思闭环、exp5 多轮迭代、exp8 AMLSim。
- 已产出结果文件：baseline_v2、blind_sweep、elliptic、amlsim_refinement、account_gnn_v2 等。
- HAN 元路径复用邻接矩阵问题（最大技术债）仍未确认修复——论文若涉及 HAN 需先验证。

**下一步（用户问"怎么进行"）**：给完整行动路径，分三阶段：①技术债清点+补实验 ②论文骨架+方法章节 ③投稿。

## 国创赛专项报告（2026-08-28 00:23）
**任务**：用户要打国创赛（中国国际大学生创新大赛2026），要求功能+前端美观性优化报告。

**国创赛关键信息**：报名 8/10-9/15，校赛评审 9/17，高教主赛道（AI+数字技术融合、突出公安创新特色），报名平台 cy.ncss.cn，一等奖5000元/二等奖3000元。

**现状核实（重要更新）**：
- **HAN 元路径已修复**！`eval_framework.py:230 baseline_gnn_han` 用 `builder.get_meta_path_adjacency()` 构造各元路径独立邻接矩阵（meta_np dict），FraudHAN 接收 meta_t dict，注释标注 "Stage2 fix"。之前记忆"HAN 元路径复用同一矩阵"已过时——**论文可写真异构 HAN**。
- alert 表名是 `alert_records`（非 alerts），后端 crud.py 正确引用 AlertRecord，接口正常（之前"查询失败"是自查脚本表名错）。
- 数据：229案件/31团伙/101用户/1171日志；后端 12 routes + 3 Agent + GNN 8实验组。
- 前端：18页面，KeepAlive 3ms，分包主入口158KB，暗黑cyan科技风已完成。

**报告产出**：`docs/国创赛2026专项提升报告.md`
- P0 演示叙事：一键演示模式/研判步骤时间线/推荐案例卡片
- P1 功能断点：研判结果端到端、报告导出、WebSocket实时预警
- P2 数据规模：扩到500案件、数据来源脱敏声明
- 前端：Showcase改"指挥中心大屏"、Dashboard加关系图谱/桑基图/数字滚动动画、空状态统一
- 时间：3周（9/17校赛），聚焦P0

**下一步**：用户确认后执行 P0（演示叙事+指挥中心大屏）。

## Agent 复核层实现完成（2026-08-28 00:31）
**决策依据**：合成数据 Hard 场景 KMeans 0.9368 / Semantic 0.9367 / HAN 0.9154 / GraphSAGE 0.3043（塌缩）——纯语义判断已近天花板，**不改纯 Agent**，保持 GNN+多Agent混合，Agent 增强落在"复核解释层"。

**新增 `backend/agents/gang_reviewer.py`（353 行，纯增量不碰 GNN 主链路）**：
- **Skill A 并案依据解释器** `MergeEvidenceExplainer`：为每个团伙生成"共享账户/话术指纹/资金链路"三要素人话解释。规则降级（`_collect_evidence` + `_rule_based_explain`），LLM 可用时增强（deepseek-chat，温度 0.3）。
- **Skill B 误并案探测器** `WrongMergeDetector`：规则级（诈骗类型互异且无共享实体→标可疑）+ LLM 级（要求输出 JSON 数组）。
- **统一入口** `review_gangs`（async）+ `review_gangs_sync`（同步包装，供 orchestrator.process 同步路径）。
- **集成点**：`cluster_agent.run()` 成功产出 gangs 后追加 `result["review"]` / `result["explanations"]` / `result["llm_enabled"]`，失败静默跳过不中断主流程。

**验证**：规则降级路径全部通过（Skill A 正确解释共享账户+话术指纹；Skill B 正确标出"杀猪盘+公检法"误并；review_gangs_sync 同步可用）。LLM 路径：本地 dev profile 强制 DISABLE_CLOUD_LLM=1 所以 get_llm_client 返回 None（设计如此：数据不出域默认），docker-compose 演示环境 `${DISABLE_CLOUD_LLM:-0}` 才启用。

**遗留**：前端尚未展示 review/explanations（需在后端 API 暴露后前端调用，见国创赛方案 7.3）；论文消融实验待加"解释覆盖率/误并修正精度"指标。

**重要发现**：orchestrator.py 实际用 LangGraph StateGraph（第4-12行注释说明 2026-07-26 重构用 LangGraph 真做反思闭环），与旧文档"没用 LangGraph"不符——**以代码为准**，论文技术稿须按"LangGraph 编排"写。
