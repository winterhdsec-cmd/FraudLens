# 2026-09-20 工作日志

> 上午的《答辩 PPT 正文口播化》一节记在 `2026-09-19.md` 末尾（当天开工时该文件已存在，就近追加）。

## 一、先补昨天的欠账：第 5 轮修复未提交
- 检查工作区发现**昨天第 5 轮（浏览器视觉验证）的修复全部没提交**：
  `WorkbenchView.vue`（asArray 渲染崩溃修复）、`_verify_seed_consistency.py`（I18/I19）、
  `fix_seed_consistency.py`（演示文案审计）、两个 memory 文件。
- **教训：UI 类改动验证完要立刻提交。** 隔夜后工作区混杂着未提交改动，
  很容易误以为"已经推上去了"（上次提交是 cc09877，之后的工作全悬着）。
- 回归确认 8/8 全绿（219 项）后提交为 `c0f29a0`。

## 二、全路由渲染体检（新增能力）
- **动机**：昨天的崩溃是"**只有带 `?case_id=` 进入才崩**"——这类雷不会出现在任何接口测试里，
  必须逐页真开一次。于是把昨天的 CDP 工具推广成**全路由体检脚本**。
- 做法（`fl_route_sweep.js`，零依赖 CDP）：
  · 遍历 19 条路由 + 3 条带参路径（`?case_id=` / `?gangId=`），共 22 次真开页面
  · 每页收集：未捕获异常、`console.error`（**错误边界的崩溃走这里，只监听 uncaught 会漏**）、
    `/api/` 的 4xx/5xx、是否落到错误边界、主区域文本长度
- **结果：22/22 全部干净**（无崩溃、无报错）。说明工作台修复生效，其他页面无同类问题。
- **环境坑**：本机有 `http_proxy=http://127.0.0.1:52463`，导致
  ① `curl 127.0.0.1:5003` 误报 502；② Chrome 也会走代理 →
  必须 `curl --noproxy '*'` 且 Chrome 加 `--no-proxy-server`。

## 三、审计「后端有实现、前端够不到」的接口
- 两条线索都做了：
  · `api.js` 导出函数扫描 → 83 个，25 个从未被引用（多为重复封装，视图直接调了 `api.get`）
  · 后端 122 条 `/api` 路由 vs 前端引用 → 0 条完全够不到（前缀匹配太宽松，结论偏乐观，仅供参考）
- **真正有收获的是逐个实测状态机**（见下）。

## 四、发现并修复：HITL 复核流程的状态机缺陷
- **线索**：库里 `review_tasks` **全部停在 `pending`**（只有 2 条）——
  说明 `assign` / `resolve` 这两步**从来没被走过**。没走过的状态机最容易带病。
- 写了带负向路径的探针，实测出**两类问题**：

### 问题 1（合规）：已完成的复核可被重新分派 / 再次定论
- 对已 `resolved` 的任务再 `assign` → **HTTP 200**，状态被打回 `assigned`，
  而 `review_result` 仍留着 → 出现「**已分派却已有结论**」的自相矛盾状态。
- 对已 `resolved` 再 `resolve` → **HTTP 200**，`review_result` 被**静默覆盖**成新结论。
  → 复核结论是留痕证据，可被覆盖等于闭环失效。
- **修法**（`routes/workflow.py`）：新增 `REVIEW_TERMINAL_STATUS = ('resolved','rejected')`
  与 `_review_status_cn()`；`assign` / `resolve` 入口做**终态门控**，命中返回 400 并提示原结论。
- **顺带修掉一个会吞异常的坑**：`api_assign_review` 只有 `except Exception`，
  没有 `except HTTPException: raise` → 在里面 `raise HTTPException(400)` 会被兜成 **500**。
  （`resolve` 有这一句，所以它的 400 一直正常。）**新增 400 校验前先确认有没有这一句。**

### 问题 2（功能缺口）：`assignReview` 有封装但没有任何 UI 入口
- `api.js:580` 有 `assignReview`，但**全项目无人调用**；复核操作列只有「详情」「处理」。
- 后果：任务永远出不了 `pending` →「**已分派**」这个筛选页签**永远是空的**。
- **修法**（`WorkbenchView.vue`）：
  · 操作列补「分派」按钮（终态隐藏），`ElMessageBox.prompt` 填复核人 → 调 `assignReview`
  · 详情由**原始 JSON 弹窗**（`ElMessageBox.alert(JSON.stringify(row))`）改为正经渲染：
    状态条 + 分派信息 + **研判快照**（pre 格式化 JSON）+ **复核意见留痕表**（追加式）
    + 补充意见输入框（用上此前从未被调用的 `addReviewOpinion`）
- **同样的 JSON 弹窗毛病**昨天在冻结工单详情上已修过一次 —— **这是本项目的一个复发模式**，
  见到 `ElMessageBox.alert(JSON.stringify(...))` 就应当视为"未完成的详情页"。

## 五、新增回归护栏
`_verify_review_flow.py`（**27 项**，R1–R9）：自建测试任务跑完整生命周期，
含两条**反向断言**——「已完成的任务不得再分派」「不得再定论（防结论被覆盖）」。
套件 8 → **9 个脚本**。连续两次运行结果一致（27/27），无抖动。

### 5.1 测试侧的大坑：用 `db.session` 读接口写入的结果会读到旧快照
- 现象：`resolve` 接口返回 200、且随后的 R6/R7 能正确看到 `resolved`（返回 400），
  但我自己的断言读出来状态仍是 `assigned`、`review_result` 仍是空 → **3 项假失败**。
- 原因：`db.session` 是 **thread-local** 的，TestClient 在**另一个线程**里跑 ASGI
  应用并提交事务；主线程这条 session 可能仍持有旧快照，`db.session.rollback()` 也不保证
  一定读到最新（本轮实测就是读不到）。
- **正确做法（已固化进脚本）**：断言落库结果一律走**独立连接**读取：
  ```python
  with db.engine.connect() as conn:
      row = conn.execute(T("SELECT status, review_result FROM review_tasks WHERE review_id=:r"),
                         {"r": rid}).fetchone()
  ```
  写/删同理用 `with db.engine.begin() as conn:`。
- **判据**：如果"接口返回 200 但断言读不到"，先怀疑**读的方式**，别急着改产品代码。
  （本例正是先通过 R6/R7 的反向断言反推出产品侧写入是好的。）
- 另：`DELETE … WHERE review_id=:x` 在 REPEATABLE READ 下会对该唯一索引取 gap lock，
  测试里自建/清理数据时注意别与待测写入撞上。

### 5.2 两条容易写错的断言
- 加复核意见只把 **`pending` → `in_review`**；已经是 `assigned` 的会保持 `assigned`
  （前端筛选页签只提供 待分派/已分派/已完成，并不使用 `in_review`，故这是符合现状的行为）。
  断言必须按实际语义写，否则会得出"功能坏了"的错误结论。

## 六、本轮改动文件
后端：`routes/workflow.py`（复核终态门控 + HTTPException 透传 + 状态中文名）
前端：`src/views/WorkbenchView.vue`（分派按钮 + 复核详情对话框 + 追加意见）
新增：`_verify_review_flow.py`、`run_verifications.py` 注册
工具（仓库外，`~/./workbuddy/binaries/node/workspace/`）：`fl_route_sweep.js`、`fl_review_check.js`

## 七、待办
- `backend/tools/freeze_executor.py` 止付冻结仍为 Mock（待警务对接，不可自修）
- 7 个后端文件 >800 行（`gnn/eval_framework.py` 1465、`routes/workflow.py` 已 1300+、
  `agents/chat_agent.py` 1150）——复赛前不动，赛后拆
- 可继续巡检的同类状态机：机构派单（`/api/dispatch`）、预警处置（`/api/alerts/{id}/resolve`）
