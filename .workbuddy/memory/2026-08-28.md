# 2026-08-28 工作记录

## 已完成的改进

### 1. Agent 复核层（gang_reviewer.py，353 行）
- 并案依据解释器（Skill A）：三要素人话解释
- 误并案探测器（Skill B）：规则降级 + LLM 增强
- 集成到 cluster_agent.run()，异常不中断主流程
- 规则降级路径测试通过

### 2. 方案文档更新
- 国创赛方案新增第七节"Agent 增强：并案复核层"

### 3. Showcase 指挥中心大屏改造 + 溢出修复
- 右侧从 260px 小网络图 → 4 大指标卡 + 动态网络图 + 预警滚动条
- **布局溢出 bug 修复**：command-deck 被撑到 3694px、SVG 拉伸、预警条占满屏
  - 根因：`alerts-slide` 超长 nowrap 文本撑爆 grid `1fr` 轨道（`1fr` 默认 `min-width:auto`）
  - 修复：`.hero-content` 列改 `minmax(0,1fr)`；`.command-deck` 加 `min-width:0` + `grid-template-columns:minmax(0,1fr)`；`.deck-graph-canvas` 由 `flex:1` 改固定 `height:260px`
  - 删左列旧 `hero-stats` 块（与大屏 deckMetrics 重复）+ 清理 heroStats/animatedStats/animateHeroNumbers 死代码
  - 验证：puppeteer dump 布局，deck-metrics 3694→591px，4 列各 141px，截图三要素齐全

### 4. 后端登录修复（僵尸进程根因）
- 改对 key.env 密码后登录仍 1045 Access denied，但 pymysql/SQLAlchemy 单测都通
- 根因：**旧后端 PID 31220 未被杀**（Git Bash 里 `taskkill //F` 被 MSYS 路径转换破坏报"无效参数//F"），占着 5003 用旧密码服务，新进程 bind 失败
- 修复：PowerShell `Stop-Process -Id 31220 -Force` 强杀 → 重启新后端 → 登录返回 access_token
- **教训：Git Bash 杀进程用 PowerShell Stop-Process，不要用 taskkill //F**

### 5. 环境配置
- key.env 重建为最小合法配置（pydantic schema 字段，DB 密码对齐根 .env）
- main.py 通过 `load_dotenv('key.env')` 注入；pydantic Settings 自身只读 cwd 的 `.env`（已删空文件）

## 视觉验证结果（截图确认正常渲染）
- Showcase 大屏 ✅ 229/31/12秒/91.5% + LIVE 关系图 + 实时预警流
- Dashboard ✅ 229/31/¥5161.6万/0 + 风险/状态双环形图
- Overview 案件总览 ✅ 229/31 + 案件类型环形图 + 案件卡片流
- Groups 团伙画像 ✅ 团伙卡 + 基本信息 + 能力评估条
- 前端 build ✅ 8.23s；后端 5003 + 前端 5173 正常运行
- 注：Dashboard 初次截图显示 "-" 是登录失败（僵尸进程）连带，非前端 bug

## 未完成
- 前端展示 review/explanations（需后端 API 暴露）
- 论文消融实验指标

## 重要更正
- **qwen3.8-flash 视觉能力实际可用**（本轮成功读取多张截图并定位布局溢出 bug），之前"不支持读图"结论作废——截图驱动的视觉迭代工作流成立：改代码 → puppeteer 截图 → Read 看图评价 → 再改

## 前端可视化修复第二轮（2026-08-28 14:30，截图驱动）
用 puppeteer 登录 + dump「canvas 是否存在」定位三处真 bug，逐一修复并验证：

### 1. Dashboard「团伙能力画像」雷达图不渲染（ECharts 漏注册）
根因：`useFraudLens.js` 按需引入 echarts 只 `use([BarChart,LineChart,PieChart,...])`，**没注册 RadarChart** → 控制台 `[ECharts] Series radar is used but not imported`。
修复：import + use 补 `RadarChart`；另把雷达容器 `v-if`→`v-show`（保证 init 时机容器已挂载）。验证：canvas=true，7 维雷达正常。

### 2. Overview「涉案金额趋势」空 + cases 全 0（两 bug）
- 趋势图数据源 `dashboardData.monthly_trend` 只有 dashboard 页才 load → Overview 单独进入时为空。修复：`loadDashboard()` 完成后若 `route.name==='overview'` 触发 `initCharts()`；`reloadCasesAndGangs` 末尾 cases 到位后也触发；`initCharts` 加入导出；OverviewView onMounted 补 `loadDashboard()` + `nextTick(initCharts)`。
- **自埋严重 bug**：上一轮在 `mapCaseFromResponse` 用了 `formatCaseAmountText` 但**忘 import** → cases.map 抛 ReferenceError → 案件列表全空（一度被误判为登录 token 问题）。修复：utils import 行补 `formatCaseAmountText`。教训：新增函数引用必同步检查 import。

### 3. 金额显示全错（单位混乱）
根因：后端金额单位是**元**（51616112 元 = 5161.6万），但 `totalAmountFormatted` 把元当万输出 `¥51616111.9万`；案件卡/表直接显示原始数字。
修复：`totalAmountFormatted` 改用 `formatAmountRaw`；案件映射加 `amountText`（`formatCaseAmountText`，元优先回退原始串）；Overview 案件卡/表/团伙卡（双¥）改 amountText；Dashboard 表金额列内联 formatAmountRaw。验证：¥3461.2万 / ¥89.0万 / ¥12.8万 全对。附带：立案时间 ISO → `.slice(0,10)`。

### 关键教训
1. **puppeteer dump「canvas 是否存在」比截图更快定位图表 bug**（截图只看到空白，dump 能区分"没渲染"vs"渲染但容器 0 尺寸"）。
2. 后端 dashboard **冷启动 ~53s**（Redis 没跑走内存，首次全表聚合）——演示前务必先 curl 预热 `/api/dashboard`，否则前端 6s 内拿不到数据图表全空。内存缓存 TTL 600s。
3. Git Bash 里 `taskkill //F` 被路径转换破坏，杀进程用 PowerShell `Stop-Process -Id X -Force`。

## 第三轮：复核解释层接入 Groups 页（"昨天的计划"收尾项）

- **后端**：`GET /api/gangs/review-results?use_llm=0|1`（rules 模式 0.1s 毫秒级、缓存 300s；LLM 模式走开关）。修复 `enable_llm=False` 仍调 LLM 的 46s bug（`False` 哨兵区分"显式禁用"与"未注入 None"）。顺手修解释文本金额 `765700.0` → `_fmt_amount` 千分位 `765,700`。
- **前端 state**：useFraudLens 加 `gangReview`/`gangReviewLoading`/`gangReviewUseLlm` ref、`gangReviewMap`/`suspiciousMergeMap`/`suspiciousMerges` computed、`loadGangReview`（缓存 TTL rule=180s/llm=300s）；进入 groups 路由（onMounted + route watch + onLoginSuccess）自动加载。
- **前端 UI**（GroupsView.vue）：每卡片加"并案依据（AI 复核）"section（解释文本 + 规则引擎/LLM生成标签 + 共享实体/回流闭环/证据链/冻结账户/话术指纹徽标）；页头加刷新按钮 + 规则/LLM 开关 + "已检查 31 个团伙"状态；可疑误并告警横幅（橙）/未发现告警（绿）。
- **去重决策**：规则模式 explanation 本身已含 rule_lines 内容（后端 summary 拼接），UI 仅在 source==='llm' 时展示 rule_lines 明细，避免重复。
- **验证**：puppeteer 截图确认 31 卡片全渲染、90 证据徽标、无横向溢出（docWidth 1600）、API 200。

### 第三轮教训
1. **Bash 工具里 `nohup ... &` 起的后端进程会随工具 shell 会话退出被回收**——长驻服务必须用 `run_in_background: true` 的 Bash 任务托管（本次因此后端"假死"了一轮：curl HTTP 000 但 docs 曾 200）。
2. 后端重启后前端截图 401 + 零数据 ≠ 前端 bug——先 curl 验 token/API，再怀疑代码。groups-shot.js 脚本登录逻辑正常，问题是后端进程已死。
3. dev server（5173）HMR 会自动热更新组件改动，验证 UI tweak 不必等 build；但最终交付仍要 `npm run build` 确认产物。

## 第四轮：内置 Redis（方案 C）——告别 Docker Desktop

用户诉求："Redis 不能直接集成到项目内吗？必须 Docker Desktop 吗？" → 结论：Redis 是 C/S 独立进程，pip 包只是客户端；但可把 **Windows 便携版二进制随仓分发 + 后端启动时自动拉起**。

### 实现
- `backend/vendor/redis/`：tporadowski Redis 5.0.14.1（GitHub Releases zip，2.5MB 必需文件，BSD 可分发），git 已纳管（`?? backend/vendor/`）
- 新模块 `core/redis_embedded.py`：`ensure_embedded_redis()` 幂等引导——socket 裸探测 `PING→PONG` 三分支（redis 复用 / open 非 Redis 占用跳过 / free 则 Popen 拉起）；`atexit` 注册清理（redis-py `SHUTDOWN SAVE` 落盘，不依赖 redis-cli.exe）；仅 Windows + 本机 REDIS_HOST + 无哨兵时生效，任何异常只 WARNING 不阻断
- `main.py` 在 `load_dotenv` 之后、中间件之前调引导；`config.py` 加 `REDIS_AUTOSTART`/`REDIS_EMBEDDED_MAXMEM`；本机 key.env 置 `REDIS_AUTOSTART=1`；`.gitignore` 排除 `vendor/redis/runtime/`（dump.rdb/日志不入库）
- `redis_pool.py` 两处逻辑修正：① `get_redis_pool()` 原来"localhost 一律视为未配置"→ 永久内存兜底，改为 `ping_ok` 探测式；② 内置模式下收紧 connect 超时至 0.5s（消除全链路 2s×N 超时雪崩）

### 两个关键兼容坑（本轮最贵的教训）
1. **Windows 版 redis-server.exe 命令行不吃 `--save "300 10"`** → `Invalid argument: Not enough parameters`，进程秒死、日志为空难排查。去掉 `--save`（默认周期 save + 退出 SHUTDOWN SAVE 已够）。手动前台跑一次二进制立刻暴露根因。
2. **redis-py 8.x 默认发 `HELLO 3`（RESP3 握手），Redis 5.0.14 无 HELLO 命令** → `unknown command HELLO`，所有连接全挂。根治：`_resp2_kw()` 探测性 helper（inspect 签名，redis-py<5 自动跳过）给 `redis_pool.py` 全部 4 个客户端构造点 + `redis_embedded` shutdown 加 `protocol=2`——对任意版本 Redis 服务端通用（6/7 也支持 RESP2）。

### 效果实测
- 后端启动：**84s → 1~6s**（原来全链路反复吃 Redis TCP 超时）
- dashboard 冷算 **~53s → 0.015s**：`SHUTDOWN SAVE` → dump.rdb → 新进程 Redis 启动自加载，`persist_probe` key 跨两次重启存活实锤
- 冒烟回归全绿：gangs 0.062s / review-results 0.031s / dashboard 命中 0.000s / 限流 Redis 路径连发 6 次正常
- 生产端不变：docker-compose.yml 的 redis:7-alpine 照旧（开发演示机=内置，服务器=容器）

### 专利/答辩可用表述
"分级缓存自适应：外部 Redis → 内置便携 Redis（探测式自动拉起）→ 进程内存兜底，任一层缺失不中断研判业务；SHUTDOWN SAVE + RDB 快照实现缓存跨重启常驻"——对应代码即 redis_embedded.py + redis_pool.py 降级链。
