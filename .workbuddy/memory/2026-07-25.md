# 2026-07-25 项目深度梳理（FraudLens 论文/专利方向）

## CTF 副线2：无声审稿（隐写，2026-07-25）
- 输入：C:\Users\hd\Downloads\无声审稿 (1).zip → peer_review.txt（纯文本）。工作目录 E:\FraudLens\ctf\，解包 silent_review/。
- 类型：三层隐形字符隐写。三通道：①review-token=Tag字符(U+E0000..)解码为ASCII JSON协议说明书；②style-token=32个变体选择符(U+FE00..)→nonce_hex=c6f13d0a9b7e42d18a5c60ef31b42877（two nibbles per nonce byte）；③margin-notes=U+2062=0/U+2064=1/忽略U+200B→512位RHS。
- 协议：seed=sha256(b'spring-review:'+nonce_hex.encode())[:16]=066d5b93fd7e6e7b3a1822b81fe7871f。构造512×336 GF(2)矩阵：每行 degree=3+(BLAKE2s(seed||i)[0]%14)，列=unique(BLAKE2s(seed||i||counter)的16-bit块 mod 336)；解 A x=rhs 得336位flag→42字节。
- 关键坑：i 编码必须 struct.pack('>I',i)(4字节大端)、counter 用 '>H'(2字节大端)、16-bit块大端、列去重；错用其他编码则方程组数十行不一致无法解。正确编码下 inconsistent rows=0。
- FLAG = flag{17b8bbaf-1e0d-4d40-8a22-8c39c6e58bb0}。交付：E:\FraudLens\ctf\无声审稿_WP.md + final_silent.py（纯标准库复现）。

## CTF 副线：云栈密令（逆向，2026-07-25）
- 输入：C:\Users\hd\Downloads\云栈密令.zip → CloudSeal.hap（鸿蒙应用）。工作目录 E:\FraudLens\ctf\。
- 路线：HAP 解包→modules.abc.pa(ark_disasm侧车)拿到 deriveWindSeed 算法；cloud.profile.cloud.wind=3f0a9cc14d62e837b5f19a047bd2c6e0 算 seed=9c668242d4972980aae361c3f64f556a（decoy.wind 是诱饵）。
- libcloudseal.so：CloudSeal_NativeCheck 两段式——FirstLoop(SBOX+旋转+置换 index_i 得 out[16])、G(SecondLoop, NEON GF(2^8) SPN，仿射, 不读 seed)、终判 result^C(0x2e0=d4de8e2794e0857281de975faa10bc7c)==0。
- 求解：用 Unicorn 做 G 的 oracle，验证 G 仿射(40/40)→提取 128×128 布尔矩阵 M 与常量 c→GF(2) 高斯消元求逆得 out_target=07d4869d996f0df64c693651abd12e12(已 verify G(out_target)==C True)→逆 FirstLoop 得 in=f35b8ad62c514a8593c00e6279347b1a→flag{f35b8ad6-2c51-4a85-93c0-0e6279347b1a}。完整函数仿真返回 1(PASS)。
- 交付：E:\FraudLens\ctf\云栈密令_WP.md（详细中文 WP）+ final_solve.py（一次跑通）。工具：隔离 venv(capstone/pyelftools/unicorn)。坑：全函数仿真须设 LR trampoline(b #0 自环桩)否则 ret(br x30) 时 x30=0 抛 UC_ERR_EXCEPTION。

## 项目定位
- 项目名 FraudLens，反诈智能研判系统，作者韩冬（湖北警官学院信息技术系），指导教师待填。
- 理论基座：中国人民公安大学《融合大语言模型和语义聚类的电信网络诈骗引流话术文本分析》论文。
- 目标：毕业设计/论文 + 期刊或会议论文 + 发明专利。

## 关键发现（文档 vs 代码严重不一致）
1. **支撑材料.md** 描述的是 6 智能体流水线（预处理/分案/分析/聚类/画像/汇总）+ BGE+UMAP+HDBSCAN 语义聚类 + **双路径串并案（语义聚类 + Jaccard 实体重叠）**。
2. **实际代码**（backend ~2.5万行）：orchestrator 为 4 阶段（规划→预处理→分析→聚类→反思），agents 仅有 orchestrator/analyst/cluster/chat 四个文件；未见独立的 Profiler/Triage/Summary Agent。
3. **ClusterAgent**：优先跑 GNN（GraphSAGE/HAN 异构图 + Louvain/深度聚类），失败回退 UMAP+HDBSCAN。即 GNN 为主、语义聚类为兜底，与 readme（只讲 GNN）和支撑材料（只讲 BGE+UMAP+HDBSCAN）都不同。
4. **Jaccard 实体重叠串并案路径未实现**：grep 仅在 rag/knowledge_base.py、rag/reranker.py 作为检索相关性出现，团伙发现代码里没有"按手机号/银行卡 Jaccard 推荐串并案"的实现。支撑材料的核心卖点之一在代码层缺失。
5. **性能报告**（2026-06-29）显示 analyze/cluster 类重接口成功率仅 55%（65/145 报错），/health、/api/cases 为 100%。"生产级/一键部署" 说法暂不成立，需先稳定。
6. demo 数据：demo_案件材料.docx、demo_演示导入材料.docx、reports/ 下大量生成的 case_*.docx；data/datasets 存在但**未确认是否有带真值团伙标签的评测集**。

## 给用户的输出
- 交付了"项目真相地图 + 论文路线图 + 专利路线图 + 立即行动清单"。
- 核心建议：先定一个统一的技术叙事并让代码对齐（二选一：补 Jaccard 实体路径使文档成立，或按真实代码重写支撑材料），再谈论文/专利。

## 二次深读（用户纠结 GNN+LangGraph 混乱）
7. **LangGraph 是空壳**：全项目无 `import langgraph` / `StateGraph` / `compile()`，仅在 4 处注释里出现（checkpoint.py / agent_runtime.py / chat_agent.py / orchestrator.py 的 docstring）。真实实现是手写顺序编排器（orchestrator.py 的 process()）。用户"要设计 LangGraph 多智能体"的焦虑是误会——无需补 LangGraph，编排器已能跑。
8. **GNN 是真资产且已落地**：gnn/graph_builder.py 构建 case/victim/phone/scam_type/city 异构图；gang_detector.detect() → 建图 → _train_and_embed()（GraphSAGE 邻接重构自监督 / HAN+GraphCL 对比学习）→ 社区检测。路线扎实。
9. **HAN 元路径是假实现（关键缺陷+优化点）**：_train_han 中 4 条元路径 case_victim_case/case_phone_case/case_type_case/case_city_case 全部复用同一 adj_tensor（注释"简化：使用相同的邻接矩阵"），异构图注意力没真正生效。修复此点既是 bug 修复也是论文贡献。
10. 顺带：实体重叠（phone/victim/type/city 边）其实已编入异构图结构，只是没作为独立"Jaccard 串并案推荐"输出暴露。

## 用户决策更新（第三次回合）
- 论文自己写，普刊即可，不强求核心期刊；不要求我代投。
- 本回合核心诉求=理清项目思路、明确项目定位（已锁定官方定义写入 MEMORY.md）。
- 给出"官方项目定义 + 真实三层架构 + 真假对照表 + 可照抄的项目自我介绍"。

## 第四次：WorkBuddy 专家中心真实清单（纠正我之前脑补的"GNN专家/专利专家"）
- 专家注册表缓存于 `~/.workbuddy/app/cache/experts/manifest.json`（333 个专家 / 14 个分类，远程市场快照 2026-07-10）。
- **重要更正**：目录里**没有真正的 GNN/图神经网络专家**（命中"图谱"的是营销身份图谱、游戏节点，均非 GNN）。HAN 元路径等核心技术深度只能用户自学或主 Agent 落地，不能在专家中心找 GNN 专项。
- 与本项目对口、真实存在的专家（已写入 `FraudLens_项目背景与信息规范.md` 对接表）：
  - 学术选刊顾问团 v3.0(04-DataAI) 选刊；长文档写作与改稿专家·福帮手(06) 论文写作；AI痕迹消除专家(06) 去AI味；
  - 企业法务专家团(11) 专利/IP；法律合规审查员·律守正(11) 合规；腾讯云天御反诈专家·天御金融反诈(13) 反诈业务；
  - 智能体编排师·调度达(10) 多智能体/LangGraph；AI提示词工程专家(04) prompt；软件架构师·架构通/后端架构师·磐石石(02) 架构；
  - API测试专家·接口探 / 性能测试专家·压测测(10) 修55%接口；实验追踪管理者·实验通(10)/哈勃AI评测专家(13) 评测。
- 产出文件：`E:/FraudLens/专家目录.md`（全量 333 专家目录）、`E:/FraudLens/FraudLens_项目背景与信息规范.md`（给专家看的项目背景+信息规范+对接表）。

## 第五次：反诈业务视角代码核查（天御反诈专家角色）
- 图节点实际仅 5 类：case/victim/phone/scam_type/city（graph_builder.py L69 注释声称含 bank/app，但 build_graph 未创建 → **无资金/账户/卡农节点**）。
- 案件间相似边 _calculate_similarity 权重=类型0.4+城市0.3+金额0.3，**无任何话术语义/文本嵌入边**；BGE 仅用于分析层。所谓"图融合语义相似"在构图层不成立。
- 节点特征：victim/phone/type/city 均 hash(name)%128 one-hot（近随机），仅 case 含金额/风险/年龄有效特征 → GNN 表征信号弱。
- **反诈结论**：当前模型是"受害者侧同类案件聚合（按类型+城市+金额）"，不是"犯罪网络发现"。真正串并案靠违法者重叠（同话务员/收款账户/话术模板），系统目前既不能表示也不能输出。且无时间边（campaign 突发性未建模）。
- 黑灰产覆盖：电诈（刷单/杀猪盘/冒充客服/虚假贷款）最适配但仍缺资金侧；职业背债/贷款包装/反催收**完全未建模**（需中介-壳公司-银行内鬼关系图）。
- 一线可用性缺口：产出=案件簇+类型/城市/金额统计，缺"可冻卡清单/可抓层级/资金去向"等可行动情报。
- 可落地改造（学生工程量）：①加 bank/account/perpetrator/time 节点+收款账户重叠边+话术嵌入相似边；②修 HAN 元路径或降级为 GraphSAGE；③用"串并案真值"做评测（4基线+消融）。

## 第六次：用户决定先搞代码（资金链方向，Stage 1 落地）
- 用户确认按民警"主打资金链"建议，先把代码搞定（非文档对齐）。选定 Stage 1：补 account/perpetrator 节点+资金流转边+收款账户重叠边+合成数据生成器+节点特征去 hash，不动 HAN 训练。
- 已落地改动（均经冒烟测试验证）：
  - `backend/gnn/graph_builder.py`：build_graph 增加 account/perpetrator 节点（case 扩展字段 accounts/perpetrators）；新增 share_account/share_perpetrator 重叠边 + fund_flow 账户间有向时序边（方向存边属性，主图保持 nx.Graph 无向以零改 HAN/社区接口）；节点特征去 hash（account/perpetrator 用统计特征）；加 schema 校验（_expect_fund 标记，规避旧缓存/库维度错位 R1）；新增 _add_relation_edge 合并关系标记（修复同团伙 case 既共享账户又共享违法者时 share_account 被 share_perpetrator 覆盖的 bug）。
  - 新建 `backend/gnn/synthetic_data.py`：可复现合成案件+资金流生成器（带真值团伙标签），供图构建与四基线+消融评测。
  - `backend/gnn/gang_detector.py`：_generate_gang_info 提取社区内 account/perpetrator 节点，输出 related_accounts/related_perpetrators（修复资金链节点被静默忽略 R2）。
  - 新建 `backend/smoke_stage1.py`：冒烟测试（importlib 加载绕过重型依赖链）。结果：account(15)/perpetrator(6) 节点、has_account(40)/has_perpetrator(48)/fund_flow(10)/合并关系边(140) 均生成，account 特征非全零，向后兼容路径不建资金节点。
- 待办：detect() 的 GNN 端（torch）需在 Docker 完整后端环境验证（隔离 venv 缺 torch）；Stage 2（真修 HAN 异构元路径+资金回流闭环+可冻卡清单+置信度门控）待 Stage 1 在 Docker 跑通后推进。

## 第七次：需求→代码开发流程总览（规划转验证）
- 用户要求整理所有需求并转成代码开发流程来验证开发（代码开发流程专家 persona 激活）。
- 产出 `E:/FraudLens/FraudLens_需求与开发流程总览.md`：六类需求清单（项目目标/业务反诈/技术架构/实验评测/文档专利/接口稳定）+ 分阶段可独立验证任务表 + 开发规矩 + 建议执行顺序。
- 任务系统重建：#7(Docker验收Stage1) #6(真修HAN异构) #12(资金回流闭环) #9(可冻卡+门控) #5(评测集固化) #11(四基线) #8(消融) #10(指标报告) #14(文档对齐) #13(论文差异化+专利草稿) #15(修55%接口) #16(话术相似边双通道)。旧#1-#4(Stage1四步)已completed。
- 验证驱动顺序：#7 → (#6,#12,#9) → (#5,#11,#8,#10) → (#14,#13) → #15 → #16(可选)。

## 第八次：继续开发 — 评测体系落地（Phase 2 启动）
- 环境探测：Docker可用(v29.2.1)但无运行容器、redis未起 → #7(Docker验收Stage1)阻塞，转做评测体系(沙箱可验证、论文命门)。BGE模型本地在 backend/bge-large-zh-v1.5。缺 torch/sklearn/hdbscan，已装 sklearn+hdbscan。
- 新建 `backend/gnn/eval_framework.py`：四基线(KMeans/HDBSCAN-only/纯语义/当前系统Louvain) + 资金链消融(去资金链图对比) + 指标(NMI/ARI/成对F1) + 困难场景(cross_gang_account_share)支持；torch/BGE缺失时语义基线退TF-IDF代理并标注、GNN基线跳过标注；结果落 eval_results.json。
- 验证结果（seed=42, 5团伙×8案）：
  - 理想(cross=0)：CurrentSystem(资金链) F1=1.000 vs 去资金链 F1=0.686 → 资金链增益+0.314（实证Stage1价值：资金链主导串并案、消解假阳性）
  - 困难(cross=0.2)：CurrentSystem F1=0.760 vs 去资金链 0.686 → 增益+0.073（跨团伙账户共享干扰下资金链图略降，结论仍成立）
  - KMeans/Semantic(属性/语义特征)两场景均1.000（合成数据干净）；HDBSCAN-only理想0.927
- 任务状态：#5(评测集固化)✅ #10(指标+报告)✅ #11(四基线)进行中(KMeans/HDBSCAN/语义已跑，当前GNN基线待torch) #7阻塞(Docker未起) #8(消融)部分(去资金链已做；去反思/去门控需orchestrator flag待补)。
- 后台安装 torch+transformers(任务uUxQuW)以启用当前GNN基线 + BGE真语义基线，完成后下轮补满四基线。

## 第九次：补满四基线 + GNN塌缩发现 → 推进 Stage2(#6)
- torch2.13+cpu/transformers/sklearn/hdbscan 均装入隔离 venv（backend/gnn/envs 处）；BGE 真语义基线启用（F1=1.000）。
- **关键 bug 修复**：`detect()` 原签名未传 `accounts_tx` → 真实推理链路 build_graph 不建资金边，Stage1 改造在 detect 路径掉线。已在 `gang_detector.detect` 增加 `accounts_tx` 形参并传入 build_graph（回归风险：旧无参数调用方需同步，但参数有默认 None，向后兼容）。
- GNN 基线实现：因 `gnn.gang_detector` 导入链拉入 openai/deepseek 等重型后端依赖（pydantic_settings→openai 无底洞），改为精准加载干净模块 `gnn/gnn_model.GraphSAGE` + 自监督邻接重构训练 → 案件嵌入 KMeans(已知团伙数) 作"当前GNN"基线；并在 notes 标注 HAN 为残实现、Stage2 修复中。
- **重要发现（论文命门）**：朴素 GraphSAGE（邻接重构自监督，与生产 `_train_graphsage` 同目标）在合成数据上**塌缩成 1 簇（F1=0.304）**，远差于 Louvain 结构社区发现（F1=1.000/0.760）。说明当前 GNN 嵌入退化、不增反损——这正是 Stage2 真修 HAN 异构元路径的强动机。
- 新增消融项 `remove_gnn`（去GNN：资金图节点特征直接 HDBSCAN 降级路径）F1≈0.43；`remove_fund_chain`（去资金链）F1=0.686。四基线+两消融现已全绿。
- 任务状态：#11(四基线)✅ #8(消融)部分(去资金链/去GNN已做；去反思/去门控需orchestrator flag待补) #5✅ #10✅。#7(Docker验收)仍阻塞(无容器)。
- **下一步**：实施 #6 真修 HAN 异构元路径（graph_builder 加 get_meta_path_adjacency 为每条元路径构不同子图邻接；_train_han 用真元路径矩阵；修 GraphCLTrainer.pretrain 硬编码 case_victim_case/case_phone_case 取增强视图改通用遍历），修完重跑 eval 看 GNN 是否翻身。回归风险 R4(旧HAN模型不可加载需重训) R5(Louvain依赖无向图) R6(回流闭环需单测)。

## 第十次：#6 真修 HAN 异构元路径 — 完成并验证
- 改动文件：
  - `graph_builder.py`：新增 `get_meta_path_adjacency()`，为每条元路径构 **不同拓扑** 的 N×N 二值邻接（含自环）：case_account_case(经收款账户2跳)/case_perpetrator_case(经违法者)/case_type_case(辅助)/case_city_case(辅助)。旧实现是同一 adj 复制4份。
  - `han_model.py`：①`FraudHAN.meta_paths` 默认键改为新资金链感知4键；②`augment_graph` 支持元路径字典(edge_drop作用每条/feture_mask只作用特征/subgraph作用共享节点)；③`pretrain` 改遍历全部元路径、增强视图传完整 meta_path_adjs（修原硬编码 case_victim_case/case_phone_case 且仅传单路径 mp1/mp2 的 bug）；④**修 SemanticAttention.forward 真 bug**：beta=[M,1] 直接 expand 成 [M,N,in_dim] 会报"expand 不能加维度"，需 unsqueeze(-1) 再 expand —— 此 bug 导致旧 HAN 根本跑不通；⑤补 `from typing import Any`（augment_graph 用 Any 但未导入）。
  - `gang_detector.py`：①`detect()` 加 `accounts_tx` 形参传入 build_graph（修 Stage1 在推理链掉线）；②`_train_han` 改用 `self._build_meta_path_adjs()` 取真元路径矩阵（新增该 helper，从 graph_builder 取并转 tensor，含兜底单位阵），并加元路径变化失效校验（current_meta_paths != model.meta_paths 则强制重训）；③删旧"复制4份 adj"代码与"简化：使用相同的邻接矩阵"注释。
  - `eval_framework.py`：加 `baseline_gnn_han`（真异构 HAN+GraphCL预训练→案件嵌入 KMeans），并在顶部加 `core.logger` 桩避免 core/__init__ 拉入 openai；四基线现为 KMeans/HDBSCAN-only/Semantic(BGE)/CurrentSystem(fund)，GNN 基线两项：CurrentGNN(GraphSAGE朴素)/CurrentGNN(HAN-true)；消融：remove_fund_chain/remove_gnn。
  - 新增 `smoke_han.py` 冒烟（真元路径前向+预训练）。
- **验证结论（seed=42,5团伙×8案）**：真异构 HAN 在理想场景 F1=1.000、困难(cross=0.2) F1=0.810，**反超** Louvain 结构基线(0.760) 且**碾压**朴素 GraphSAGE(0.304 塌缩1簇)。坐实"HAN 残实现→真异构修复"是实贡献。
- 重要旁证：旧 HAN 因 SemanticAttention bug + 4份同矩阵，**实际从未跑通**；本次是真修 + 真跑通。
- 任务状态：#6✅ #11✅ #5✅ #10✅ #8(消融部分：去资金链/去GNN已做；去反思/去门控待orchestrator flag)。剩余 Stage2：#12(资金回流闭环) #9(可冻卡清单+置信度门控)。#7(Docker验收)仍阻塞(无容器)。

## 用户学习路线（零基础前置知识，2026-07-25）
- 用户自述 ML/GNN 方向零基础，直接看代码看不懂。已给五关最小前置路线（约3周）：第0关 Python读代码级(3-5天,通关=看懂synthetic_data.py) → 第1关 图论直觉+一点线代(3天,通关=看懂graph_builder.py) → 第2关 ML最低限:训练/聚类/NMI-ARI-F1(4-5天,通关=看懂eval_framework.py) → 第3关 GNN直觉:消息传递/GraphSAGE/HAN元路径(1周,通关=看懂han_model.py骨架) → 第4关 LLM与多智能体常识(2天,通关=讲清orchestrator流水线)。
- 教学原则：以项目自身代码为教材、每关绑定通关标志、资源刻意少（仅3Blue1Brown线代前4集+神经网络前2集），不推西瓜书/CS224W。下一步待用户确认后从第0关开始：synthetic_data.py 逐段中文导读+自测题。
